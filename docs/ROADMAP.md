# Roadmap

Where `lex-sys` is, what got it here, and what is next. The README says
what the language *is*; this file is the only place that tracks its
history, so the README does not have to grow a paragraph per change.

Tracked in the epic: **[#1](https://github.com/alpibrusl/lex-sys/issues/1)**
— milestones with acceptance criteria, sequencing, risks and open
decisions.

---

## Milestones

| Milestone | What | Status |
|---|---|---|
| **M0** — native hello world ([#3](https://github.com/alpibrusl/lex-sys/issues/3)) | Lexer, parser, AST, IR, Cranelift backend, a real executable | **done** — green on linux-x86_64 and darwin-aarch64 |
| **M1** — typed core | Type checker, `bool`, structs, ADTs with exhaustive `match`, monomorphised generics. No linearity, no effects — deliberately | **done** |
| **M2** — the actual thesis ([#2](https://github.com/alpibrusl/lex-sys/issues/2)) | Linear ownership, effect rows and capability-passing as **one** system | **done** — §3 through §8 of [`linearity-and-effects.md`](linearity-and-effects.md), every must-reject fixture enforced |
| **M3** — minimal but real | Slices and strings, arenas, libc FFI, settled overflow semantics, canonical printer, per-unit identity, file IO through `Fs` | **done** — `examples/lines.ls` is the acceptance criterion: a tool that reads and writes files, counts and filters, and whose authority to do any of it is one narrowed capability |

Everything below M3 is post-milestone work, shipped one slice at a time.

---

## What has landed

One row per slice, newest last. Each links to the document that holds the
reasoning — this table is the index, not the argument.

| # | Slice | The claim worth remembering |
|---|---|---|
| [#22](https://github.com/alpibrusl/lex-sys/pull/22) | [A general heap](heap.md) | `Box[T]` is `res`, so §4's exactly-once rule turns out to be a **memory safety** rule for free: no leaks, no double frees, no use-after-free, none of them checked by anything new |
| [#23](https://github.com/alpibrusl/lex-sys/pull/23) | [Reading through a reference](reading-references.md) | **A reference gives references.** One rule closed two limits that looked unrelated, and there are no binding modes to infer because the scrutinee decides |
| [#24](https://github.com/alpibrusl/lex-sys/pull/24) | [Command-line arguments](arguments.md) | An effect row is about **visibility**, not containment. Arguments grant no power, and they are still an effect, because a function that branches on `--force` should say so in its type |
| [#25](https://github.com/alpibrusl/lex-sys/pull/25) | [A program in more than one file](many-files.md) | The smallest feature with the largest consequence: three design docs had each had to write *"there is nowhere to put a library"*. It also made `canonical-ast.md` §1 testable for the first time |
| [#26](https://github.com/alpibrusl/lex-sys/pull/26) | [Boxed slices](boxed-slices.md) | `Box[[T]]` is a pointer *and* a length — what every collection wants. Found a double free reachable from ordinary code |
| [#27](https://github.com/alpibrusl/lex-sys/pull/27) | [Sharing](sharing.md) | `Rc` is **not expressible**: it needs a value that copies *and* names an allocation, and every value here is one or the other. Three ways of writing it are three fixtures, refused by three unrelated rules. `Gen` is a library, and is `examples/slab/` |
| [#28](https://github.com/alpibrusl/lex-sys/pull/28) | [Tuples](tuples.md) | The first feature a **library** asked for rather than a design — and it costs nothing: a tuple and the struct it replaces emit byte-identical objects |
| [#29](https://github.com/alpibrusl/lex-sys/pull/29) | [Shadowing](shadowing.md) | The restriction was a real rule stated too bluntly. A binding may be shadowed exactly when it is dead — which is the rule assignment already had, so it is one rule with two syntaxes |
| [#30](https://github.com/alpibrusl/lex-sys/pull/30) | [Standard input](standard-input.md) | Not a seventh capability: a second **label** on `Io`, following `Fs`'s `fs_read`/`fs_write`. Which renamed `putchar`'s effect to `io_write` across the repository |
| [#31](https://github.com/alpibrusl/lex-sys/pull/31) | [Modules](modules.md) | **A module reaches no hash.** A call has encoded the callee's hash rather than its spelling since M0, so namespaces cost the identity system nothing — and a module is not a trust boundary: `pub` means reachable, never safe |
| [#32](https://github.com/alpibrusl/lex-sys/pull/32) | [A standard library](standard-library.md) | `--std`, with the source compiled into the binary rather than looked up. Found the compiler emitting **every** non-generic function rather than what `main` reaches — 6720 bytes against 1048 for a program calling none of it |
| [#33](https://github.com/alpibrusl/lex-sys/pull/33) | [Mode polymorphism](mode-polymorphism.md) | Checking a claim found a **leak and a double free**: a `val` on a generic declaration was trusted rather than checked. And a worse bug — a call took its types from one function and its effect row from another, so a call into C could declare `[]` and compile |
| [#34](https://github.com/alpibrusl/lex-sys/pull/34) | [Collections](collections.md) | Which collections hold a resource is decided by **shape**, not generics: a list works because taking it apart produces its elements; an array does not because freeing one is a single `free` that runs nothing |
| [#35](https://github.com/alpibrusl/lex-sys/pull/35) | The README | Not a language change: the README had grown a paragraph per slice in three places at once, so the history moved here and the per-example prose moved to `examples/` |
| [#36](https://github.com/alpibrusl/lex-sys/pull/36) | [A borrowed field](reading-references.md) | The double free was never about **reading**, it was about **owning**. A `res` field through a reference is a borrow, and the refusal that used to guard it was doing work the type rule already does |
| [#37](https://github.com/alpibrusl/lex-sys/pull/37) | [Slicing](slicing.md) | `s[a..b]`, and the answer to "a writer abstraction": there should not be one, because a function taking a `Writer` must declare the **union** of what every destination could do, and a union row is not an exact row. The abstraction is the buffer |
| [#38](https://github.com/alpibrusl/lex-sys/pull/38) | [`defer`](defer.md) | Sugar that **stays** sugar: expanded during lowering, so the checker replays the same events the hand-written version would, and there is one set of linearity rules rather than two. §12's question answered — "visible" here means *the type says what happened*, not "written on the line where it runs" |
| [#39](https://github.com/alpibrusl/lex-sys/pull/39) | [The authority surface](authority.md) | §12's release question answered **no**: the four `release` calls are not ceremony around the authority declaration, they *are* it — `main` owns rather than borrows, so its row is `[]` and every entry point has the same signature. `lex-sys authority` reads them instead |
| [#40](https://github.com/alpibrusl/lex-sys/pull/40) | [`[budget]`](budget.md) | Answered **no**, from the units: a budget is wall-clock seconds, commands and **cents**, none of which is a property of a program's text. `lex-os` already charges it, at the boundary that can stop you. What was wanted was legibility, so the authority report grew `--output json` |
| [#41](https://github.com/alpibrusl/lex-sys/pull/41) | [What a program can reach](reach.md) | Answered by building it: a **REST endpoint over a real TCP socket**, with no socket type, no `Net` capability and no library — because sockets are libc and libc has a name. The no's are one sentence: a foreign *result* is a scalar, so every opaque handle (TLS, libpq, `FILE *`) is out. And the row cannot say `net`, because a library is not an authority domain |
| [#42](https://github.com/alpibrusl/lex-sys/pull/42) | [What the overflow trap costs](overflow-cost.md) | The README's *"low single-digit percent"* had never been measured. Measured: +2.8% call-bound, +3.6% memory-bound, **−9.1%** branch-bound, **+40.5%** arithmetic-bound — so not a percentage but a rule. And the stated reason was wrong: not the never-taken branch, but that **a trap is observable, so the loop cannot vectorise** — clang pays 46% and gcc 74% for the same guarantee. Both documents corrected in place |
| [#43](https://github.com/alpibrusl/lex-sys/pull/43) | [Bits](bitwise.md), and [a port](porting.md) | The first program here that **already existed**: coreutils `base64`, byte-for-byte on both directions. It needed the bit operators and hex literals and *nothing else* — no capability, no library, no change to linearity or rows. And running the suite under load for an unrelated reason found a real bug in `examples/serve/`: one `read` returns what arrived, not what was sent |
| [#44](https://github.com/alpibrusl/lex-sys/pull/44) | [A port with resources](porting.md#9-the-second-port-sort) | `LC_ALL=C sort`, five owned resources on the heap, checked against GNU. Answered §6's four untested things: the move loop costs three tokens rather than difficulty, effects concentrate at the edges (four of eight rows are `[]`), and `borrow mut` never got in the way — five nested blocks did. Found **four** missing library functions, every one absent because nothing had asked |
| [#45](https://github.com/alpibrusl/lex-sys/pull/45) | [Against C and Rust](against-c-and-rust.md) | The measurement `overflow-cost.md` §4 said was owed: **1.6× at equal semantics**, on both a compute-bound and a memory-bound kernel, with Rust within 4% of C — so the gap is the backend, not ownership. And the numerical row: f64 is **17% slower** than the fixed point lex-sys is forced into, so the missing `float` costs precision (10 orders of magnitude) rather than speed |
| [#46](https://github.com/alpibrusl/lex-sys/pull/46) | [What a checked row is worth](purity.md) | The answer to *is there anything it does better?* — **yes, exactly one thing**: the row is a checked purity proof, which C can only promise unchecked and Rust cannot state. 35% of functions here qualify; worth **1.94×** as CSE and **158×** with hoisting. Collected by nothing: Cranelift has no call attribute, and an own optimiser is a non-goal — now an *informed* one |
| [#48](https://github.com/alpibrusl/lex-sys/pull/48) | [Floating point](floating-point.md) | `float`, IEEE-754 binary64 in full. The interesting decision is §2.1: NaN and infinity do **not** trap, because wrapping lies about a value where NaN announces the absence of one — and a per-operation trap would defeat exactly the loops floats are for. `truncate` traps on what C leaves undefined. Writing it found a real collision the design had waved away: `t.0.1` |
| [#49](https://github.com/alpibrusl/lex-sys/pull/49) | [Printing a float](float-printing.md) | The shortest decimal that reads back to the same bits, and it is **library code**: `std/fmt.ls` is written in lex-sys, row `[]`, no `Heap`, and the compiler's whole contribution is `bits_of` — one bitcast. `std.bignum` underneath has no division, because the one quotient a printer needs is a single digit and nine subtractions settle it. Checked against Rust's `{:e}` over 9000 values, which found two real defects an example never would; §3.4 records the three where Rust and Python disagree and why both are right. A comment claiming 2200 bits was measured at 1080 and corrected |
| [#50](https://github.com/alpibrusl/lex-sys/pull/50) | [Compile-time evaluation](compile-time.md) | The answer to *can it be faster than C?* began with finding we were **slower at the easiest thing there is**: `2 + 3 * 4 - 14` is zero and emitted fifteen instructions, because a checked add is `sadd_overflow` + `trapnz` and Cranelift's folding rules are written for the plain form — `overflow-cost.md` §3.2's mechanism sighted a second time. Folding it in the front end brings that to parity; a **certain trap is now a compile error** rather than a `SIGILL`. Past C in one place only: clang gives up on recursion, so `fib(23)` is a runtime call at `-O2` and a constant here. And the estimate that motivated the work was wrong — 65 foldable calls predicted, **15** actual, because pure-and-constant is necessary and not sufficient |
| [#51](https://github.com/alpibrusl/lex-sys/pull/51) | [Compile-time data](compile-time-data.md) | A `static` item: a body with no parameters and no run time, evaluated during compilation into read-only data. `examples/base64/` drops its 64-entry per-character scan and decodes **6.1×** faster. But the measurement corrected the pitch: `compile-time.md` §8 called this "the one that pays" and **5.7× of the 6.1× was already writable** in 36 lines of plumbing. What the feature actually buys is that plumbing, read-only pages, and the row that is a capability argument — 65 536 entries does not fit in a 64 KiB arena and **traps**, so a program that released `heap` could not have one at all |
| [#52](https://github.com/alpibrusl/lex-sys/pull/52) | [Layout](layout.md) | The first slice here to build **almost nothing**, on purpose. This roadmap said packing reaches C and transposing goes past it; measurement says the second half is wrong. Transposing is worth **1.43×** in lex-sys and **1.31×** in C — the same transform, the same size, so it moves both along rather than closing a gap, and the vectorised half of the prize needs a vectoriser `overflow-cost.md` §3.2 already showed we do not have. Packing is real (2.6× on a byte-heavy struct) and **3 of 83** struct fields here are `byte` or `bool` — and `slab`'s `Entry` would not shrink at all. So: `lex-sys layout`, which makes the deferral falsifiable instead of remembered |
| [#53](https://github.com/alpibrusl/lex-sys/pull/53) | [Against a wider set](benchmarks-game.md) | Three programs from the Computer Language Benchmarks Game — fannkuch-redux, spectral-norm, binary-trees — each checked against the answer the Game publishes. The **1.6×** headline was two kernels' midpoint; across five it is **1.17× to 2.58×**, and it tracks how much of the run is in code Cranelift generated. Porting found a **compiler crash**: two *sibling* `region` blocks put the backend's arena stack at length 0 where index 1 was wanted, latent since arenas landed because nothing here had two regions side by side. And `scripts/game.py` reports the **spread**, which no published ratio here had carried |
| [#54](https://github.com/alpibrusl/lex-sys/pull/54) | [Bulk output](bulk-io.md) | `benchmarks-game.md` §2.1 parked this as "a real finding and a separate slice", and the finding is not the **12.8×**. `examples/serve/` had been writing whole slices for four slices, through `Ffi("libc")` — which `reach.md` §5 says is every authority at once. So a program that wanted to print quickly had to ask for the filesystem, the network and `exec`, and a program that asked only for `Io` was held to one byte per call: **the cheap thing to grant was the expensive thing to run**, which is the wrong lesson for a capability language to teach every time someone profiles. Fixed in the primitive rather than the library, because a function holding `&!i Io` can only call what `Io` authorises. It lowers to `fwrite` and not POSIX `write`, since `putchar` is buffered and a raw descriptor write would interleave wrongly. And §4 declines the headline: base64 gains **1.6×**, not 12.8×, because 6 ms of the saving goes straight back into byte-at-a-time buffer fills that `cc -O2` vectorises away. **§4.1 then corrects §4 itself** (#55): it had said a program "whose time is output" gets most of the 12.8×, and `examples/sort/` — which writes 9 MB and moved onto the bulk path with no change to the program at all — gains **1.22×**. Volume written is not time spent writing, and the gain is only ever the share of the runtime that was call overhead |
| [#55](https://github.com/alpibrusl/lex-sys/pull/55) | [Canonical identity, made observable](canonical-ast.md#8-not-yet-contracts) | Asked what it would take to put lex-sys code in lex-lang's `lex-vcs`, and the answer was that **81%** of that crate is already language-agnostic — it keys on `String` ids and `BTreeSet<String>` effects — while lex-sys's own hashes are the part that is not ready. So this slice builds the falsifier rather than the integration. `lex-sys-id` had 61 tests and **all of them were relational**: they compare two hashes to each other, and not one said what a hash *is*, which left §8's "not frozen across releases" a claim nobody could observe. 35 golden fixtures now pin it, reporting by fixture name so an insertion does not cry wolf — and the first thing they caught was one of my own fixtures: perturbing the `BINARY` tag moved `unary`, because `0 - a` is a subtraction and there is no unary minus, so that fixture had never covered `UNARY` at all. And §8's field-order entry turned out **wrong in both halves**: a struct literal was never a gap, because the checker *refuses* `P { y: 2, x: 1 }` rather than reordering it (`defined-behaviour.md` §3 — the order you read is the order it runs); the destructuring pattern's gap was real but the stated reason was not, since sorting by name settles it with no declaration lookup and also survives a struct reordering its own fields. The binders sort with the names, because they are positional from there on and a collision would be worse than the difference removed |
| [#56](https://github.com/alpibrusl/lex-sys/pull/56) | [UTF-8](utf8.md) | Asked how far lex-sys is from a string type, and the answer was already written: [`strings.md`](strings.md) settled it as `&r [byte]` with no encoding claimed. So the question became the library, and the probe says **the language needs nothing** — a forty-line decoder matched GNU `wc -m` exactly on the first compile (119 bytes, 79 code points over ASCII, Latin, Japanese, emoji and a combining sequence), and `vec.Vec[&t [byte]]` compiles, so `split` can return substrings bound to the text they came from. What the probe did find is the decision `strings.md` §1 said would have to be paid somewhere: §1 declined a validated string because it would have to say what an invalid one *is* — an error, a replacement character, an unrepresentable state — and a **decoder cannot decline**. Seven malformed fixtures, four implementations, **four different answers**, and GNU contradicts itself by counting `f5 80 80 80` as one character though it encodes a value above U+10FFFF. So GNU is not the oracle here, which also means `wordcount.ls` must never be checked against `wc -m`. §3 takes strict validity and the Unicode maximal-subpart skip rule, verified to reproduce Python's replacement counts on every fixture — and `std.utf8` now implements it, agreeing with **Rust's own** `from_utf8_lossy` and `str::from_utf8` over 2,000 generated cases. Nothing in the design moved on contact with the code, which is the first slice here where that is true |
| [#57](https://github.com/alpibrusl/lex-sys/pull/57) | [The rest of the string library](standard-library.md) | [`utf8.md`](utf8.md) §1 said `split`, `trim`, `join`, ordering and case are "code, not design". Rather than invent the surface, `examples/cut/` was ported to **ask** for it — the route `vec.set` and `vec.swap` took — and it asked for exactly two: `bytes.count_byte` and `bytes.field`. `trim` came with the field parser and `bytes.compare` came from `examples/sort/`, which had the rule inline. Four functions, three call sites, checked against GNU `cut` on eight field specs. Porting it found the **64 KiB arena as a line-length limit**: a 65 000-byte line buffer beside a 1 025-byte bitmap traps on exhaustion, so the number in the source is what fits rather than what was wanted, and the comment says so. And it found `standard-library.md` §5.3 claiming a fix it had not made — `wordcount.ls` moved its word boundary to `std.bytes`, **`tally.ls` kept a byte-identical copy**, so "one definition, in one place" was two definitions that happened to agree with nothing checking. Corrected in place, in the entry that named the failure mode |
| [#58](https://github.com/alpibrusl/lex-sys/pull/58) | [Standard error](standard-error.md) | Two documents cited *"`reach.md` §6's standard-error gap"* as a blocker and **`reach.md` never named it** — five rows, none of them this, and the second citation copied from the first. What the absence cost, measured: every failing program here produced **zero bytes on every stream**, against GNU's 64, 68 and 22 for the same three failures, and eleven of nineteen examples have a non-zero exit. The workaround is worse than silence twice over — a diagnostic on the output stream fed a line of prose to the `sort` downstream of it (`wc -l` answers 1 where GNU answers 0), and standard output is **fully buffered when it is not a terminal**, so a message written just before a trap is lost: 0 bytes to a file and 0 through a pipe. Closed as a **third label on one capability** — `standard-input.md` §2's rule a third time — with `err_write` named for the stream because `1>` and `2>` are two redirections, and §2.1 states plainly what that widens and why the answer is that the grant is the capability while the gate is the row. Five diagnostics across `sort`, `cut` and `base64`, four of them in the suite against the system's own GNU tool. And **§1.3 corrects itself**: the first draft said the failing path was untested, and it was tested — against the status this program happened to have, while the same test compared eight valid specs to the real `/usr/bin/cut`, because a silent program gives a test nothing to compare. `cut` exited 2 where GNU exits 1, under a comment claiming it used GNU's |
| [#59](https://github.com/alpibrusl/lex-sys/pull/59) | [Refusals a machine can read](agent-errors.md) | lex-lang's `AGENT_GUIDELINES.md` is a contract with a reader that is a *program*; lex-sys had none. Measured: across 196 must-reject fixtures the checker produced **125 distinct message shapes, 101 of them exactly once**, reported **one error per invocation**, and offered no machine-readable form — so classifying a refusal meant matching 125 English patterns by regex, one compile at a time. Closed with a **52-rule catalogue**: a stable `rule` tag on every `Diagnostic`, set at the error site rather than derived from the sentence, plus `check --output json` and every independent refusal reported rather than the first (checking is already total and per-declaration, so those bodies are independent; a parse error still ends the file). §2 is the part worth reading: the `rule_tag` half of lex-lang's answer transfers whole, and `suggested_transform` **does not** — its own guidelines page shows it beside `rule_tag` in the checker's JSON, and in the source it lives in `lex-store`/`lex-vcs`/`lex-lsp` as an attestation against an op id, which is machinery lex-sys deliberately does not have. A `fix` field was designed here and **cut**, because the argument for it is about a consumer nobody has watched. And §3.2 is the finding: the conformance harness has claimed since M0 that *"adding a rule means adding a fixture"*, which was unfalsifiable while nobody had enumerated the rules — it was **43 of 52**, and `every_rule_has_a_fixture` now keeps it |
| [#60](https://github.com/alpibrusl/lex-sys/pull/60) | [AGENTS.md](../AGENTS.md) | [`agent-errors.md`](agent-errors.md) §7's own next row. lex-lang's contract says a downstream repo copies `AGENT_GUIDELINES.md` as `AGENTS.md`; lex-sys is a different language with different rules, so it needs its own — and it had **none**. Measured: **90,283 words across 42 documents** and no first page, so an agent asked to write lex-sys could read all of it or guess. Written from evidence rather than taste — the rules are ranked by how many fixtures they have (`linear-value-unconsumed` 19, `effect-not-declared` 12, `reference-escapes-region` 11, `mode-bound-violated` 11), and §6's six facts are the ones that each cost this repository a compile or a SIGILL while porting a real program. `lex-sys agent-guidelines` prints it from inside the binary, the way `--std` carries the library. **Every checked code block is a fixture**: the valid ones compile, the refused ones are refused with the rule they name, and changing one rule name to a wrong one turns the suite red — checked by doing it |
| [#61](https://github.com/alpibrusl/lex-sys/pull/61) | [A line reader, answered no](line-reading.md) | The roadmap asked for `std.lines` because *"two programs hand-roll the same loop"*. Reading them says **one does**: `tally.ls` keeps nothing, `wordcount.ls` keeps nothing, `base64.ls` buffers its *output*, `sort.ls` keeps the whole input — only `cut.ls` reads a line at a time. A documented **no**, and the useful part is what the row was pointing at. `examples/cut/` read a line into a 60,000-byte arena slice and the source called that *"truncated"*; it is worse, because a truncated line loses its **delimiters**, so `cut` switched to the no-delimiter rule and printed **60,001 bytes of the wrong field with exit 0** — a silently wrong answer, in a program checked against GNU on eight field specs, none of which varied the one dimension that mattered. Fixed by growing on the heap: **1.18×** for correctness (152.9 → 181.1 ms on 14.7 MB), and the program's authority report gains `heap`, which is `bulk-io.md` §3.2's rule meeting its mirror and not contradicting it. One function was earned — `buffer.clear`, because reusing the allocation is **13.0×** faster than `drop` plus `empty` per line, so it is a gap rather than a nicety |
| [#61](https://github.com/alpibrusl/lex-sys/pull/61) | [GPU, and whether `lex-gpu` should exist](gpu.md) | Asked whether lex-sys could target a GPU, how a native one would work, and whether the GPU dialect should be its own language. The first two are design; the third looked like taste until the numbers came in. Same reduction, six ways, **SIMD instructions counted in the emitted code rather than inferred from the clock**: a **bounds check is free** (1.01×, SIMD unmoved at 10) and an **overflow trap is not** (1.46×, SIMD 10 → 0). So the memory half of the no-undefined-behaviour claim survives a GPU at no cost and the arithmetic half cannot — a much smaller concession than it first looked. And the one that settles the language question: **deleting the trap does not reach C.** lex-sys without traps is still scalar, 2.27× off vectorised clang and 1.55× off *scalar* clang, so the trap is 1.83× of its own cost and everything left is Cranelift — which means LLVM is what buys GPU speed, equally, whichever language the kernels are in. §2.1 **corrects `overflow-cost.md` §3.2** in place: it measured one guard and generalised to "the check" |
| [#62](https://github.com/alpibrusl/lex-sys/pull/62) | [`sqrt`, and the capability question](float-math.md) | [`floating-point.md`](floating-point.md) §7 had carried *"`std.math` over floats — the capability question has to be settled first"* since `float` landed. The capability question turns out to be **the wrong question**: `sqrt` is *one instruction* (`sqrtsd`, `fsqrt`), so it reaches no library, needs no `Ffi`, and its row is `[]` — arithmetic does not acquire a capability after all. What decided it was measuring the two programs that hand-rolled a square root, which is how the bar was met (`newton.ls` and `benches/game/spectral.ls`, counted by reading them). **Both are wrong**: `spectral.ls`'s twenty-step Newton loop is not correctly rounded on **58.4%** of 40,008 values and is wrong by **143 orders of magnitude** on a large one — 10^300 comes back as 4.77 × 10^293, because the first guess is `x/2` and twenty halvings do not cross 143 decades. The benchmark never noticed: it only ever asks for the root of something near 1.27, which is `cut`'s long line again — the dimension the test never varied. So this is a **builtin rather than library code**, which is the opposite of the call `float-printing.md` made, for the same reason: put it where it can be correct. A correctly-rounded root is not expressible in lex-sys; the shortest decimal is. `sin`, `exp` and `log` stay open, and float `abs`/`min`/`max` are refused on §1's rule — one asker with a working alternative. And **AGENTS.md §6 was wrong**: it said there is no unary minus, and there is; the table that said so was the one part of that document the suite did not check, which it now does |
| [#63](https://github.com/alpibrusl/lex-sys/pull/63) | [`&!` is not unique, and cannot cheaply become so](aliasing.md) | Answers **no** to the question `README.md` asks two sections after its own table had settled it. Across the 82 programs this repository builds, making `&!` mean Rust's `&mut` refuses **one fixture** — written to document the behaviour being changed — so the stated cost, *"it refuses programs that compile today"*, was wrong, and the real price is elsewhere. There are **three** ways to alias a `&!`; two close syntactically and the third, a reference returned from a call, needs provenance in signatures, which is lifetimes and the borrow checker the non-goals exclude by name. And the fact would have **no consumer** even if proved: Cranelift has no `noalias`, only three fixed WebAssembly alias regions. §6 is the two conditions that would reopen it |
| [#63](https://github.com/alpibrusl/lex-sys/pull/63) | [Every check the backend emits, priced](check-cost.md) | Shipped in the same PR, and it **corrects two documents at once**: `overflow-cost.md` §3.2 said *"the check"* having measured one, `gpu.md` §2.1 narrowed it to the overflow check having measured two, and **six of eight** loop-body checks take the SIMD count to zero. The axis is neither memory safety nor arithmetic — `s[lo..hi]` and `s[i]` are both bounds checks and cost **2.16×** and **1.00×**, and the same two comparisons on the induction variable cost 0.99×. **Provable is free, and loaded is not.** The worst check in the language is `truncate(f)` at **3.35×**, which nothing had looked at |
| [#64](https://github.com/alpibrusl/lex-sys/pull/64) | [Poison, and where the thesis lives](poison.md) | Whether a per-lane flag reduced at a boundary costs less than a trap, which `gpu.md` §5.1 had left as one number. It is three. On a wide vector ISA poison is a large win for every **per-element** check — `a << b` and `-x` become free and `truncate(f)` drops from **3.28× to 1.33×**. On the overflow check **carried by a reduction** it does not help and is worse, because four lanes compute four different partial sums, so the flag records a different question. A control kernel falsified the first explanation offered for that: the barrier is the *spelling*, since the same test written as sign logic vectorises. And on baseline SSE2 the whole win disappears, so the answer is a property of the target rather than of the semantics |
| [#65](https://github.com/alpibrusl/lex-sys/pull/65) | [File handles, and three names that cost more than the design](file-handles.md) | `open_read`, `file_read`, `file_close`, a `res` `File` and a three-constructor `Read`, so the end of a file and a failure stop being two meanings of `-1`. The milestone was **smaller than `filesystem.md` §3 advertised** — two of its three questions were already answered by machinery that exists, and a forgotten `file_close` is a compile error with no new rule. Building it found three things the design had not: `Result[File, int]` **cannot be a builtin's signature** (`Result` is `std`, and `std` is opt-in), the label carries **no path** because the path is spent at `open_read`, and three new prelude names cost **31 fixtures** plus the `extern fn read` in `examples/serve/` |
| [#66](https://github.com/alpibrusl/lex-sys/pull/66) | [`sort` through a handle: the I/O went, the time did not](file-handles.md) | The port that tests the previous row. Reading through a handle takes **1.00×** the I/O the old path took 2.75× of, and the wall clock moves **0.932×** — three times less I/O bought 7%, which is `bulk-io.md` §4.1's correction arriving from the reading side. What handles are actually worth is the code they delete: an 8 MiB ceiling and the exit status that existed only to report it |
| [#67](https://github.com/alpibrusl/lex-sys/pull/67) | [The backend, audited rather than remembered](backend-limits.md) | Five documents gate the roadmap's largest row on four claims about Cranelift, and only one had ever been read from the source. All four hold, checked against `cranelift-codegen` 0.121.2. The fifth fact is why it was worth writing down: `is_call()` and `can_trap()` are **adjacent clauses of one four-line predicate**, so this project's checked purity proof (worth 158×) and its defined behaviour (costing 3.35×) are blocked by the same function. Also: `sadd_overflow` is *pure* — it is the `trapnz` beside it that pins the loop — and Cranelift's SIMD is an **input** language for WebAssembly, with no pass that makes vectors out of scalar code |
| [#68](https://github.com/alpibrusl/lex-sys/pull/68) | [Effect polymorphism, answered no](effect-polymorphism.md) | The roadmap's last language row, and the only one with no document behind it. **537 functions here write a row and not one could be polymorphic in it** — not *does not need to be*, could not. A row is fixed at the declaration and read straight off it by every caller, and the two things a function is generic over, types and regions, cannot select a callee. The nearest asker, `io.write_all` against `io.error_all`, differs by the **builtin** as well as the row, so collapsing it needs a function value: row polymorphism is the shadow of higher-order code, which `Rule::NoFunctionValues` refuses with a fixture |
| [#69](https://github.com/alpibrusl/lex-sys/pull/69) | [The hash rate, measured](hash-stability.md) | The number `canonical-ast.md` §8 said nothing was measuring, and the one the `lex-vcs` row below was gated on. It is two numbers and they disagree. The encoder moved **20 times** and the golden fixtures observed **none** of them, because they landed after the movement stopped — the instrument has never been exercised. The *language* moved enough that **71% of this repository's own 117 `.ls` revisions** no longer type-check under today's build, and a single label rename (`io` → `io_read`/`io_write`) is **42%** of that. A hash-keyed VCS inherits the second rate, so what to wait for is a **plateau**, not a number |
| [#70](https://github.com/alpibrusl/lex-sys/pull/70) | [A character, written as a character](character-literals.md) | `'a'` is the integer 97: a third spelling, after decimal and hexadecimal, so no new node kind and no moved hash. **121 places** in this repository spelled an ASCII character as a decimal number, and three of them already carried the translation **in a comment** — the clearest evidence a notation is missing, since the information exists, a human typed it, and nothing checks it against the number beside it. Counting decided the type rather than taste: **91 of the 121 sites want an `int`**, because `putchar` and `getchar` mirror libc, and the commonest site is `48 + n % 10`, seventy-five times. So it is a *spelling, not a type* — `bitwise.md` §1.1's rule applied a second time — and §2.1 states the half it does not buy: the 30 `byte` sites keep their `byte_of` and gain only a readable argument. Two sites keep their numbers, the vertical tab and the form feed, because the escape set is `strings.md` §4's and neither has one. **This row also fixes the table above**: six slices had landed without one, and two rows linked to the wrong pull request, because a PR carrying two slices was given two consecutive numbers |
| [#71](https://github.com/alpibrusl/lex-sys/pull/71) | [The checks, read out of the binary](emitted-checks.md) | `check-cost.md` is subtitled *"what **every** check this language emits costs"* and it measured **C**: each kernel in `benches/guards.c` carries a comment naming the lex-sys check it stands for, and not one had been read against the machine code. **Six of the eight proxies are exact** — the author knew the unsigned trick and used it. The two that are not include the headline: the C guard tests the float range with two `ucomisd` on every element where Cranelift emits `cvttsd2si` and one `cmp $0x1`/`jno`, because the conversion answers a sentinel that `rax - 1` overflows on — so **3.35×** prices a guard this language does not emit, and *the worst check in the language* does not survive as a ranking. Division is backwards twice: §5 said both operators emit nothing, and each emits exactly one comparison, **not the same one** — `1 / 0` dies with **SIGILL** from its own `ud2` and `1 % 0` with SIGFPE from the hardware. `int::MIN % -1` **answers 0**, where `defined-behaviour.md` §2.3 said it traps, borrowing the quotient's reason for an operator that produces no quotient — the compiler was right and the rule was wrong. And the **constant folder disagreed with the backend** about exactly that, so one expression was a compile error written down and a 0 computed. Writing the probe also found a **codegen bug**: a folded `byte`-returning call put an `int`-shaped literal where the backend expects one machine byte, and `int_of(g(65))`, `g(65) == byte_of(66)` and returning it each failed the Cranelift verifier with no span and no rule tag. Five documents also had the check's *name* wrong — it is `truncate(f)`; `int_of` widens a `byte` and emits no check at all |
| [#72](https://github.com/alpibrusl/lex-sys/pull/72) | [Flags](flags.md) | This list carried *"flag parsing"* under **ordinary work, blocked by nothing** on the assumption that the two programs which parse flags would each be a little shorter with a library. They are not too long. **They are wrong**: of the twelve spellings GNU accepts across the two ports, **six disagreed**. `cut` refused four of them loudly — `-d ,`, `-d, -f 2`, `--delimiter=,` and a trailing `--`. `base64` answered `--decode` and `-di` by **encoding its input and exiting 0**, because `--decode` is not two bytes so the test for `-d` was false and the flag was dropped. And every base64 test in the suite passed exactly `-d`: twelve input sizes, both directions and three malformed inputs, all through **one spelling of one flag**, which is `line-reading.md` §1's shape again with the spelling of an argument in place of the length of a line. `std.flags` is a **cursor, not a `getopt_long` table**, because a table has to know which options take values and then resolve GNU's abbreviations against itself; here the program says when it wants a value, so there is nothing to search and nothing to disambiguate. Three functions — `start`, `step`, `value` — and the nine shapes of an argument. Both ports now agree with the real coreutils binaries on every spelling but the three §4 keeps on purpose, and those are asserted to **still diverge** so one that quietly starts agreeing gets moved rather than forgotten |
| [#72](https://github.com/alpibrusl/lex-sys/pull/72) | [The first page](first-page.md) | `agent-errors.md` §1 measured what the *machine* audience had and `AGENTS.md` answered it. This is the third audience — a person deciding whether the project is worth their attention — and the instrument arrived on its own: a reader outside the project wrote a fifteen-proposal design document, of which **eight were already shipped** with a document behind each. Its phases 1 through 4 are done and it opens at phase 1. A proposal that re-proposes eight shipped features is a measurement of the first page, and a better one than any this project could have invented, because nobody wrote it to be one. The cause is countable rather than aesthetic: in 572 lines the words `sandbox`, `audit` and `evidence` appeared **zero** times, `agent` in the execution sense zero (both hits were about `AGENTS.md`, the *writing* guide), and `lex-os` **once**, inside a parenthesis about narrowing — so the three proposals that live in the runtime could not have been known about, and all three were re-proposed. The shape said something else again: *"Performance expectation"* was **180 lines, 31%** of the file, the largest section by far and about the weakest thing here, while *"What exists"* was one 70-line block of prose. The rewrite adds an **ecosystem map that did not exist** — three repositories, one idea, two of them wired together, and `lex-sys` sharing **no code** with either, which is the integration the proposal assumed and this page now denies in writing. Plus the two joins as rows: `lex-vcs`, gated on a plateau, and `.ls` under a lex-os grant, not built. Nothing about the language changed. Two tests keep it honest: every local documentation link resolves — run by hand at the end of every slice until now — and the README's surface block matches `--help`, which caught `layout` and `agent-guidelines` missing from the page and `check` having lost its `--output json` |
| [#73](https://github.com/alpibrusl/lex-sys/pull/73) | [Under a grant](under-a-grant.md) | `README.md` and `authority.md` both said `authority --output json` existed *"for a supervisor checking it against a grant"*, and **no supervisor had ever been asked**. Measured against `lex-os`'s real grant — `{filesystem, network, exec}` plus an egress allowlist — one dimension is enforceable and **more precisely than the grant can express**, because `fs_read(p)` carries a path where the grant has a tri-state. **Two are not enforceable at all**: `examples/serve/` binds a TCP port, listens, accepts and answers HTTP, and its report is `args` and `ffi`. `reach.md` §5 argues correctly that the row is not *wrong*; what is new is that against `network: None` it is **unable to refuse**, and the only program here that touches the network is the one it cannot refuse. §3 removes the rescue and **corrects `reach.md` §5.2**, which said the foreign symbol list tells a supervisor what it is running *"without trusting a word the program says about itself"*: the list is a proof about **names** and a heuristic about **domains**, because the step from a name to a domain is the declaration and the declaration is the program's to write — six lines of `extern fn syscall` type-check, open a socket and report `["syscall"]`. §4 is the diagnosis in one sentence: **`Ffi(lib)` is the one capability whose label does not bound what it authorises**, and the filesystem dimension works precisely because `filesystem.md` §2 already took files out of libc. So `reach.md` §6's `Net(host)` row is **promoted**: not a refinement of the report but the prerequisite for lex-sys code running under a grant at all. And the join itself is smaller than it looked — no compiler integration, no embedding the front end; `--output json` is already the interface |
| [#74](https://github.com/alpibrusl/lex-sys/pull/74) | [The network](net.md) | The design for the row the previous slice promoted, written before the code the way `filesystem.md` was — and the probe found that **`reach.md` §5.1's framing describes neither side**. *"A host is a thing worth narrowing to"* is the **outbound** question. `examples/serve/`, the only network program in this repository, declares `socket`, `setsockopt`, `bind`, `listen`, `accept`, `read`, `write`, `close` — pure **ingress**, and no `connect` anywhere in the corpus. Read the other side: a `lex-os` grant's network fields are `network` and `egress`, and **both are outbound**; there is no inbound notion in it at all. So the one program lex-sys has does the direction the grant does not describe, and the grant describes a direction lex-sys has no program for. §2 argues the asymmetry is **correct**: outbound is the program's choice — it picked the host, so the authority question belongs in the type — and inbound is not, because who reaches a listening port is decided by routing and never by the program, which is exactly why lex-os's second wall is **iptables on the tap device** rather than a grant field. `Net` therefore has **two axes**: `net_out(host:port)`, narrowing by prefix the way `Fs` does, and `net_in(port)` bounding a port and nothing more. §3 keeps `filesystem.md` §2's decision — the operations are **builtins**, because leaving them `extern fn` gates them by `Ffi("libc")` and `Net` contributes nothing. And §5 is the uncomfortable count this project's own rule demands: inbound **1** asker, outbound **0**. The half that unblocks the lex-os join is the half nothing here asks for, so this ends at *settled, not built*, and the next step is a program that connects rather than an implementation of a design no program has tested |
| [#75](https://github.com/alpibrusl/lex-sys/pull/75) | [Fail closed, and credit](related-work.md) | An outside audit's two cheapest correct items: the authority report now leads with `"bounded": false` for any program reaching foreign code, so a supervisor that reads nothing else refuses by default ([`under-a-grant.md`](under-a-grant.md) §5.1); and a related-work document crediting what this project took and who got there first — Austral above all, which the repository had never mentioned |
| [#76](https://github.com/alpibrusl/lex-sys/pull/76) | [The folder against the backend](differential.md) | The audit's C2(b), and the first item of its recommended order. `fold.rs` said *"`bin_traps_exactly_where_the_backend_does` checks them against a running program"* — a test that **never existed**, while the one disagreement it would have caught (`int::MIN % -1`, #71) shipped. Now every operator the folder evaluates, on every pair of 14 boundary operands, is computed three ways — folded during lowering, folded through a call, and at run time on operands neither optimiser can see: **5,156 cases, 0 disagreements**, the same 436 traps on both sides. Five deliberately broken folders were each caught, #71's bug in one case. Writing it found the fault it was not built for: `bits_of(0.0 / 0.0)` was **`0xfff8…` on x86-64 and `0x7ff8…` on aarch64**, so `compile-time.md` §6's *"the same value on every host and every target"* was false for one builtin. `bits_of` now gives every NaN one pattern — two instructions, no branch — and since it is the only operation that can see a NaN's bits, the sentence is true again, corrected in place |
| [#77](https://github.com/alpibrusl/lex-sys/pull/77) | [A program that connects](connect.md) | The second item of the audit's order, and the step `net.md` §5 named instead of building `Net`. `examples/fetch/` is an HTTP/1.0 client, and the suite points it at `examples/serve/`, so a lex-sys client fetches from a lex-sys server. It found four things. **No names**: `getaddrinfo` answers a pointer, so a program only ever connects to an *address* while a `lex-os` grant only ever names *hosts*. **The destination is data**, so `net.md` §4's literal is a bound checked at run time, as `Fs(prefix)` is, corrected in place. **`struct sockaddr_in` is `2, 0` on Linux and `16, 2` on macOS**, and both network programs work on macOS only because BSD reads family 0 as `AF_INET`. The first version said `connect` would refuse the Linux bytes, reasoned from memory, and the darwin runner refuted it. **`errno` is a pointer**, so why a connection failed is unknowable. It found no need for a socket type. Outbound askers: 1, so `Net` stays *settled, not built*. The test written to fail on this day is now a count |
| [#78](https://github.com/alpibrusl/lex-sys/pull/78) | [When the compiler is wrong](internal-errors.md) | The audit's C2(c). Measured by putting #71's bug back for one build: `check` answered **`{ "refused": [] }` with exit 0**, and `build` printed an unlocated *"code generation failed"* with **exit 3**, the code the CLI documents as *the environment failed*. So a compiler bug read as the machine's fault, and the machine-readable gate passed a program that would not build. Codegen's 22 invariant sites were bare Rust panics, exit 101. Now a backend failure is a refusal with rule **`internal`**, located at the function whose code failed, exit 1. Each function's code generation runs under `catch_unwind`, and **`check` runs the backend too**, at about 2× its old time, a few milliseconds per real program. Tested without a bug to test on, by breaking one function's IR by hand |
| [#79](https://github.com/alpibrusl/lex-sys/pull/79) | [Files, and a budget for them](../CONTRIBUTING.md) | The audit's C1. `lex-sys-ir/src/lib.rs` had reached **10,060 lines**, one slice at a time, with nothing to say no. It is now a table of contents over `ir`, `builtin`, `defs`, `function` and `lower/`, where `impl FnLowering` is split across child modules that see its private fields, and nine unit-test files declared with `#[path]` so no test changes name. **A pure move**: the same 228 test names before and after, and every line of the old file present in the new ones apart from formatting. The largest file is now 1,594 lines. **`tests/files.rs` refuses any source file over 2,000 lines** (rustc's `tidy` uses 3,000). The two files already over it, codegen (2,886) and `conformance.rs` (6,684), are held to those numbers as ceilings that may only come down. `CONTRIBUTING.md` writes down the gate, how a slice is done and the conventions |
| [#80](https://github.com/alpibrusl/lex-sys/pull/80) | [Files, and a budget for them](../CONTRIBUTING.md) | #79's two ceilings, retired. **`lex-sys-codegen`** (2,886 lines) becomes `abi`, `emit`, `layout` and `body/`, with `impl BodyEmitter` split across child modules the way `FnLowering` was. Its largest file is now 688. **The conformance suite** (6,684) becomes `tests/conformance/main.rs` and seventeen topic modules, still one binary. Its item boundaries were found with a scanner that tracks strings and comments, because embedded lex-sys sources put `fn main` at column 0. Both are pure moves: the same test names, 8 and 104, and no line of either old file missing. Two codegen doc comments were already wrong and are corrected: one still said every error there is the environment's, which #78 made false. `CEILINGS` is empty. The files nearest the line are `parser.rs` (1,997) and `lex-sys-id` (1,909) |
| [#81](https://github.com/alpibrusl/lex-sys/pull/81) | [Fuzzing the compiler](fuzzing.md) | The audit's C2(a). `tests/fuzz.rs` is a stable-Rust mutation fuzzer, part of `cargo test`: it seeds from all 331 `.ls` files, makes one to three token edits (half of them replacing a token with one **of the same kind**, so a mutant is usually a different program rather than a broken one), and requires three things of every mutant: nothing panics, what parses prints back to **the same tree**, and what the checker accepts the backend compiles. 44% of mutants parse, 9% check and 8% compile, and the test asserts floors so a shallow run cannot pass. Over 450,000 distinct mutants the checker and backend found nothing. **The printer failed four ways**: `(e,)` printed as `(e)`, turning a refused program into an accepted one; a label argument such as `ffi("a\n")` printed unescaped and did not reparse; since #43, prefix and postfix operators bound no tighter than `*`, so `-(a / 10)` printed as `-a / 10` (`UNARY` and `POSTFIX` are now derived from the operator table); and `1 . 2`, component 2 of an integer, printed as the float `1.2`, a fixed point only the tree comparison could see. `tuples.md` §2.1 said a one-tuple could not be written; it could, and `one_tuple.ls` now pins its refusal |
| [#82](https://github.com/alpibrusl/lex-sys/pull/82) | [Function values, not yet](function-values.md) | The audit's L1, written before anything forces it. **No program here asks**: of 594 function bodies, the only pair differing by nothing but its callee is `io.write_all` against `io.error_all`, the one sort has one comparator, and none of the 22 enums picks an operation. A whole program can always write a function value as an enum and a `match`, which is what it would do instead, and the fixtures measure the price: the row becomes the union of the arms' rows. The decisions: **no closures**, since a captured capability is authority no parameter names; function values, when an asker arrives, are captureless, non-generic, `val`, with an exact row in their type. That **changes no row rule and brings no row variables**, which corrects `linearity-and-effects.md` §10's prediction. Callbacks into C need row `[]` and scalar parameters, which leaves `atexit` and `signal` and not `pthread_create` |
| [#83](https://github.com/alpibrusl/lex-sys/pull/83) | [Who resolves names](net.md) | `connect.md` §1 found that a lex-sys program only ever connects to an address while a `lex-os` grant only names hosts, so whoever resolves owns the check. **Decided: the builtin**, because lex-sys has to be usable without `lex-os`: under the perimeter answer a standalone program could use no names and nothing would narrow where it connects. `connect` takes a name and a port, checks the name against its capability's bound, then resolves it. Under `lex-os` the firewall stays as the outer wall, and the perimeter writing its pinned addresses into the guest's hosts file makes both resolvers agree without DNS egress. The perimeter answer was considered first, and §4.1 says why each of its reasons does not decide it |
| [#84](https://github.com/alpibrusl/lex-sys/pull/84) | [Parse errors as JSON](agent-errors.md) | `check --output json` answered **invalid JSON for every parse error**, 20 of the 214 reject fixtures. The message was the whole rendered refusal, newlines included, and the writer escaped only quotes and backslashes. It also gave those refusals `position: null` though the span was known. Now the message is the sentence, the position is resolved, and control characters are escaped everywhere JSON is written, `authority` included. The existing test compared one line of each message, so it could not see any of this; a strict JSON reader now reads every fixture's answer. Found by replaying the repository's history through the compiler |
| [#85](https://github.com/alpibrusl/lex-sys/pull/85) | [Editions, measured](editions.md) | The audit's L2, designed before `Net` needs it: adding `net` to `Split` would break all 210 files that destructure it. `scripts/history.py` replays the repository's history through today's compiler: 58 of 141 revisions no longer check, 45 of them from the `io` split. **Reading an old `io` as `io_read, io_write` recovers none**, because rows are exact and the right half depends on each body. A checker-driven migration tool recovers 30, including 15 broken only because `Split` grew. The rest are a capability parameter and linearity tightenings, which should stay breaks. So an edition holds open only **additions**, which can simply be absent in older files. A refining change ends support for earlier editions, with `lex-sys migrate` as the fix, and a tightening is always a refusal. Edition 1 adds no bytes to identity, and edition 2 will be edition 1 plus `Net`. Nothing is built yet: the marker lands with `Net`, after `parser.rs` (1,998 lines) is split *(corrected by [#87](https://github.com/alpibrusl/lex-sys/pull/87): the marker shipped ahead of `Net`, not with it)* |
| [#86](https://github.com/alpibrusl/lex-sys/pull/86) | [Files, and a budget for them](../CONTRIBUTING.md) | `parser.rs` (1,998 lines) becomes a table of contents -- `parse`, `parse_into`, `Parser` and the token plumbing every child calls -- plus `items`, `stmt` and `expr`, with `impl Parser<'a>` split across them the way `FnLowering` was. A pure move, verified by token multiset rather than eyeballed: every token of the old file is present in the new ones, and the only tokens added are the `pub(crate)` 42 methods needed once they cross module boundaries, the three `mod` declarations, and the `impl` wrapper each child needs. The same 61 test names survive, moved to `parser/tests.rs` with `#[path]`. The largest file is now 531 lines. Prerequisite for `Net` ([`editions.md`](editions.md)), which adds a field to `Split` |
| [#87](https://github.com/alpibrusl/lex-sys/pull/87) | [Editions, measured](editions.md) | The marker itself, ahead of `Net` after all -- `editions.md` §8 said otherwise and is corrected above. `edition N;` is a per-file marker, checked once ahead of `module`/`import` the same way `static` is a contextual keyword: it is an ordinary identifier everywhere else. Absent, a file is edition 1 forever, which is why `Ast` gained an `item_editions` side table parallel to `item_modules` rather than a field nothing but this needed to know about. Only edition 1 is known today, so `edition 2;` and later are refused under a new rule, **`unknown-edition`** -- there is nothing to name yet, since `Net` and the per-edition `Split` are the next slice. Unobservable from any program a file can write until they land |
| [#88](https://github.com/alpibrusl/lex-sys/pull/88) | [A second outbound program](connect.md) | `connect.md` §6 and `CONTRIBUTING.md` rule 3 put the bar for a feature at two askers per half, and outbound had one -- so building `Net`'s outbound half now, as the roadmap otherwise had it queued to do, would have been the discipline it names quietly skipped. `examples/report/` is the second: an agent that posts a result, `POST <path>` with a body, rather than a generic client. It found nothing new about the design questions §1-§4 already settled -- but sending a body large enough to matter, which `fetch/` never needed to, found two of its own bugs: a region is one 64 KiB arena chunk, and copying a large message into one before sending it could trap; and the header buffer, once that was fixed, was five bytes short of what its own `Content-Length` digits needed. Both are bounds checks catching a real mistake, not a silent one. **Outbound is now 2 and has cleared its bar; inbound is still 1** |
| [#89](https://github.com/alpibrusl/lex-sys/pull/89) | [A second inbound program](listen.md) | Inbound's half of the same bar, cleared the same way. `examples/collect/` accepts a count of connections in a loop and reads each `POST`'s body by `Content-Length`, the two things `serve/` never had to do, streamed to standard output rather than materialised for the reason `docs/connect.md` §8 already paid for. Found nothing new in the program -- but found a real deadlock in its own test: `collect`'s stdout is a pipe smaller than a 100,000-byte body, and reading it only after the HTTP exchange finishes blocks forever the moment the pipe fills, since the exchange cannot finish until the write does. Draining it concurrently, on its own thread, is the fix. **Both halves of `Net` have now cleared the bar.** Writing a second corpus file also perturbed the fuzzer's fixed-seed mutant sequence enough to land on `fib(1_000_000)`, which overflowed the compiler's own stack: the compile-time evaluator's recursion-depth guard (`fold.rs`'s `DEPTH`, 128) was never measured against a debug build's 2 MiB test-thread stack, only against fuel. Bisected to 68 safe / 72 not; `DEPTH` is now 32, with a regression test that reproduces the constrained stack directly rather than waiting on the fuzzer to land there again |
| [#90](https://github.com/alpibrusl/lex-sys/pull/90) | [`Net`, slice 1](net.md) | Building `Net` turned out to need more than the marker #87 shipped: nothing in the compiler consumed `Ast::edition_of` outside the parser's own tests, so making edition 2 real meant threading it through every place a written name resolves, not only `resolve_type_at`. Agreed mid-implementation as three slices ([`connect.md`](connect.md) §9): this one, name resolution, then inbound. **Built:** `Net(bound)`, narrowed like `Fs(prefix)` (plain prefix, no `/`-boundary rule); edition 2's `Split`, a *second* declaration of the source name `Split` rather than a sixth field on the existing one -- `Split` is `res` by inference, so a field nothing edition-gated could unname would still be consumed by every one of the 210 files that destructure it, exactly the break an addition must not cause; `net_out`, discharged by an owned `World` the same unnarrowed way `ffi`/`fs_read`/`fs_write` already are; and `connect(net, a, b, c, d, port)`, which builds the same sixteen-byte `sockaddr_in`, Linux layout, that `examples/fetch/`'s `address` builds by hand, once, in the backend. Three more raw `defs.iter().find(name ==)` lookups turned up beside the one already fixed -- a struct literal, a variant constructor, a destructuring `let` -- each needing the same "latest declaration this file's edition can see" answer or an edition-2 `let Split { .. } = split(world)` resolved its pattern against the wrong `Split`. `examples/fetch/` and `examples/report/` are **not** ported: both still need `read`/`write`/`close` on the socket `connect` opens, which stay `extern fn` against libc until a later slice, so porting now would widen their report, not narrow it |
| [#91](https://github.com/alpibrusl/lex-sys/pull/91) | [`Net`, slice 2](connect.md) | Slice 2 of 3: `connect` changes shape from slice 1's `connect(net, a, b, c, d, port)` to `connect(net, name, port)`, the shape §4.1 actually decided -- octets were only ever a stand-in for "no resolver exists yet", and `getaddrinfo` accepts a numeric address exactly as it accepts a symbolic one, so one builtin now covers both. The bound is `"host:port"`; split once, at compile time, into a host prefix (`checked_host`, `checked_path`'s shape minus the `/`-boundary and `..` rules neither `Net` nor a host name has) and an exact port -- a narrower capability names *one* port, not a prefix of them, so that half is equality and traps before the host is even copied. First build attempt found the bug this design avoids by construction: comparing the dialled host against the *whole* `"host:port"` bound trapped on every call, since a 9-byte host is shorter than an 11-byte bound that includes its port. `struct addrinfo`'s layout (48 bytes, `ai_flags`/`ai_family`/`ai_socktype`/`ai_protocol`/`ai_addrlen`/`ai_addr`/`ai_canonname`/`ai_next`) is asserted from POSIX's `<netdb.h>` rather than measured the way `sockaddr_in`'s was, since it was standardised after both supported platforms already existed; `getaddrinfo` is asked for `AF_INET`/`SOCK_STREAM` only, and the backend patches only the resolved `sockaddr`'s port bytes -- the two bytes `connect.md` §3 already found portable -- rather than reconstructing the address itself, since the resolver already wrote the platform's own correct family bytes. `examples/fetch/` and `examples/report/` are still not ported, for the same reason #90 gives: neither gap (`read`/`write`/`close` still on `Ffi("libc")`) closes until a later slice. Fixed on darwin CI, in place: `struct addrinfo`'s `ai_addr`/`ai_canonname` swap byte offsets between glibc and Darwin/BSD's original RFC 2553 order, caught only by the darwin-aarch64 runner |
| [#92](https://github.com/alpibrusl/lex-sys/pull/92) | [`Net`, slice 3](listen.md) | Slice 3 of 3, the last of the three agreed: `bind(net, port)`, `listen(fd, backlog)`, `accept(fd)` and the `net_in` label. `bind` folds `socket`, `setsockopt(SO_REUSEADDR)` and `bind` into one call, the inbound mirror of `connect`'s `socket`+`connect`, and builds the same `struct sockaddr_in` `examples/serve/` builds by hand -- `INADDR_ANY`'s eight zero bytes where `connect`'s has four octets, since a listener binds every address the host has. `listen` and `accept` take no capability -- the fd is what `bind` already proved the authority for -- so both are ordinary fixed signatures, checked the way `Builtin::Close` is, not the way `connect` and `bind` are. A `Net` narrowed for `bind` carries just the port as text, `net_in`'s bound checked by equality the same way `connect`'s port half already is; only `bind` performs `net_in`, `listen`/`accept` perform nothing, the same reasoning `close`'s empty row has. Writing `bind`'s port check the same shape as `connect`'s surfaced a bug already latent there: a bound whose port half does not parse as a number was read the same as an *absent* port, silently permitting any port instead of refusing all of them -- both now read a malformed half as -1, a port neither builtin can ever be asked to dial, so a malformed narrowing refuses every call rather than none; this narrows what was promised, never what was reachable. Testing the new builtins against a real accepted connection found a genuine, pre-existing latent bug: every `extern fn` in this compiler crosses a lex-sys `int` as 64 bits regardless of the C function's own width, while `bind`'s internal failure-path `close` (like `open_file`'s) is declared at libc's true 32-bit width -- one linker symbol, two declared signatures, which Cranelift correctly refuses when a program declares its own `extern fn close` alongside either. Not fixed here -- `close` is one call a short-lived test process does not need, since the OS reclaims both descriptors on exit -- recorded as a known exposure shared with `open_read`/`fs_read`/`fs_write` since before `Net` existed |
| [#93](https://github.com/alpibrusl/lex-sys/pull/93) | [fasta and reverse-complement](benchmarks-game.md) | `benchmarks-game.md` §2.1's last deferred row, unblocked once the Game's own spec turned out to be reachable this session. Both are checked byte for byte against the Game's own N=1000 reference output, and the measurement **corrects a prediction rather than confirming it**: `bulk-io.md` §4.1 guessed `fasta` would land nearer `base64`'s 1.59×; it measures **0.54×** -- faster than C -- because its C reference writes with `putchar` per byte, the ordinary algorithm the Game's own rules require, while `fasta.ls` has no primitive cheaper than `io.write_all`, so the ordinary lex-sys program is the bulk one. `reverse-complement`, whose read side is `getchar` either way there is no bulk read, lands at **1.19×**, inside the range the other five programs already described |
| [#97](https://github.com/alpibrusl/lex-sys/pull/97) | [LLVM backend, slice 1](llvm-backend.md) | The first slice `llvm-backend.md` §5 (#96) named: `lex-sys-codegen-llvm`, textual LLVM IR shelled out to `clang -c`, behind an additive `--backend cranelift|llvm` flag defaulting to `cranelift`. Every leaf is one `alloca`, read and written with plain `store`/`load` -- `clang`'s own mandatory `mem2reg` does the SSA promotion this crate does not bother building. **Building it found §5's own target false**: `examples/hello.ls` is not "no arithmetic, no traps, no structs" -- `write_all`'s `n = n + 1`/`n < len(s)` is checked arithmetic and a comparison, `s[n]` is bounds-checked indexing, and the greeting is string data, none of which this slice lowers. Corrected in place, with a new fixture, `tests/accept/llvm_smoke.ls`, that is what the bullet list actually described. Every node outside the slice refuses with a located `CodegenError`, never a panic -- an opt-in backend's own gap, not the checker's invariant. Also closed §6's linux-x86_64 open row: `ud2` as inline assembly assembles to Cranelift's own two bytes and exits `SIGILL` every time, measured on a real host rather than assumed from the design doc's darwin-only session (aarch64's `udf` half stays open -- no aarch64 host in this session either) |
| [#98](https://github.com/alpibrusl/lex-sys/pull/98) | [LLVM backend, slice 2](llvm-backend.md) | Checked arithmetic: every trapping `BinOp` (`Add`/`Sub`/`Mul` via LLVM's own `with.overflow` intrinsics, `Div`/`Rem`, `Shl`/`Shr`) plus the three bitwise operators, which cannot trap. **A real correctness finding**: LLVM's `sdiv`/`srem` are undefined, not trapping, on a zero divisor or `int::MIN / -1`, unlike Cranelift's, which trap on both -- the obvious translation would have been silently wrong on exactly the two inputs this language exists to make defined, fixed with explicit checks ahead of the instruction. Every trap raises the signal #97 already measured, checked by seven tests that read `.signal()` rather than only `status.code() == None` -- §3.2's own critique of the existing suite, applied here instead of left as someone else's gap. **A methodology finding while writing those tests**: `docs/compile-time.md` §3 folds a literal-pair operator at compile time into a refusal rather than a runtime trap, so a naive test like `9223372036854775807 + 1` never reaches this backend at all -- every trap test instead routes one operand through an impure `putchar`-calling function the folder cannot evaluate. Comparisons lower too but stay untested end to end: this language has no way to observe a `bool` outside `if`/`match`, named explicitly now as the next gap. `tests/accept/llvm_arith.ls` exercises all ten non-short-circuit operators once each, the exact printed bytes proving the values rather than only that `clang` accepted the module |
| [#99](https://github.com/alpibrusl/lex-sys/pull/99) | [LLVM backend, slice 3](llvm-backend.md) | Control flow: `Stmt::If`/`Stmt::While` plus the two short-circuit operators, `And`/`Or` -- `ir.rs`'s own comment already said these belonged here rather than in `binop`'s instruction table. **No `phi` needed, despite §5's own text once assuming one would be**: every local is already an `alloca` (the first slice's own design, for `clang`'s `mem2reg`), so the block after an `if` just loads whatever the taken arm last stored, and a `while`'s back edge is re-entered the same way -- fresh loads each time, nothing carried in a register across the edge. `&&`/`||` use the same trick over a one-leaf temporary rather than merging two live values. Comparisons, built in #98 but untested since this language has no way to observe a `bool` outside `if`/`match`, are exercised end to end for the first time: `tests/accept/llvm_control.ls` prints a different byte per comparison, and two short-circuit tests prove -- by whether an impure `shout()` call's own output appears -- that `&&`/`||` actually skip their right side when they should. `hello.ls` still refuses, but the boundary moved: `--backend llvm` on it now fails on the string literal, not the `while` loop. `Stmt::Match`/`Stmt::Region` stay refused, named explicitly now rather than left implicit under "control flow" -- `Match` waits on enum layout, `Region` on arena allocation |
| [#100](https://github.com/alpibrusl/lex-sys/pull/100) | [LLVM backend, slice 4](llvm-backend.md) | Slices and strings: `Expr::Bytes`/`Expr::Len`/`Expr::Index`, plus `Builtin::IntOf` -- necessary plumbing `hello.ls` needed the same way `putchar` was necessary plumbing for #97. **`examples/hello.ls` now builds and runs through `--backend llvm`**, byte for byte the same as Cranelift -- the loop the first three slices opened (each found `hello.ls` needed one more thing than assumed) is closed. Two things came out cheaper than the design doc assumed: a string literal needs no instruction, only a declaration, since an LLVM global symbol is already a usable `ptr` constant wherever Cranelift's `global_value` needs one; and bounds checking is exactly one more `trap_if`, the same shape `checked_shift`'s range check (#98) already was. One thing did not come free: `docs/compile-time.md`'s constant folder does not reach `Index` at all, unlike its own finding about `BinOp` in #98 -- a literal out-of-range index passes `lex-sys check` and only traps at run time, so this slice's bounds-check tests use a literal index directly, with none of #98's `probe`-routed workaround. `Place::Element` (`s[i] = e`) lowers too, by the same `element_address` the read side uses; `Place::Field`/`Place::Deref` stay refused, waiting on struct/box layout |
| [#101](https://github.com/alpibrusl/lex-sys/pull/101) | [LLVM backend, slice 5](llvm-backend.md) | Structs and enums: `Expr::Struct` needed almost nothing new -- a struct value is positional already, so its leaves are just every field's leaves concatenated, the same shape `Expr::Tuple` would be, and this backend already read a struct this way through `Expr::Field` (#100). `Expr::Enum`/`Stmt::Match` matched `lex-sys-codegen`'s own layout exactly: an enum's leaves are a tag followed by *every* variant's payload, not only the constructed one -- `variant_layout` computes one variant's offset into that whole, shared by `enum_lit` (writes the tag and the constructed variant, zeroing the rest) and `bind_payload` (reads the same offsets when a `match` arm binds them). `match_stmt` lowers to a chain of `icmp eq` tag tests. **One real subtlety**: a `match` whose every tested arm returns still needs a `merge` block, unlike `if_stmt` (#99), which omits one -- `if`/`else` are syntactically exhaustive, a `match` is only exhaustive because the checker proved it, so a chain with no wildcard arm still has a fall-through the IR does not know is impossible, and it needs a legal terminator (`emit_default_return`, factored out of `emit`'s own fall-through in this slice). Deliberately not in this slice: matching through a reference and `Place::Field`/`Place::Deref`, both needing pointer arithmetic into a referent -- not needed to make `tests/accept/enums.ls` build, since its own `match` binds an *owned* `Point` read back through `Expr::Field` |
| [#102](https://github.com/alpibrusl/lex-sys/pull/102) | [LLVM backend, `-O2` and a first measurement](llvm-backend.md) | Asked to benchmark the two backends and found a bug first: `run_clang` never passed `clang` an optimisation level, so `mem2reg` -- which the whole memory-backed design leans on -- never ran, and the backend was *slower* than Cranelift on every kernel that builds on both. Fixed (`-O2`, always). Measured after: on the three `benches/` kernels that build on both backends today, LLVM is **8%-54% faster**, and on `mandelbrot.ls` it is **indistinguishable from C** (0.96x-1.00x) where Cranelift reproduces `against-c-and-rust.md`'s original 1.6x-1.8x almost exactly -- answering that document's own stated falsifier, corrected in place. **Checked, not assumed, and it is not what it looks like**: `objdump` on the `-O2` object finds **zero** SIMD instructions -- the gain is register allocation and instruction selection catching this backend up to a competent scalar compiler, not a vectoriser, and `overflow-cost.md`/`check-cost.md`'s "a trap is not reassociable" finding holds here too, confirmed on a real backend for the first time. Also inventoried, by trying to build every file under `benches/` with `--backend llvm`: three gaps not previously named on their own -- `wrapping_add`/`sub`/`mul`, heap boxing (`box`/`box_slice`), `arg_count`, `Type::Float` -- beyond the already-documented `region`/`alloc_slice` |
| [#103](https://github.com/alpibrusl/lex-sys/pull/103) | [LLVM backend, `wrapping_add`/`sub`/`mul`](llvm-backend.md) | #102's own smallest-first ordering: LLVM's plain `add`/`sub`/`mul`, no `nsw`/`nuw` requested, are already two's-complement wraparound, so unlike `binop`'s checked forms this needs no overflow check -- three match arms and a four-line helper. Every checked-vs-wrapping pair in `benches/` that doesn't also need `region`/`box_slice` now builds on `--backend llvm`. **The measurement looked broken until it was checked**: `sum_wrapping.ls` first reported **-99.1%** against Cranelift, and `objdump` explains why rather than excuses it -- `lexs_run` compiles to `xor %eax,%eax; ret`, because the loop's body is mathematically a no-op and, unlike the checked twin, nothing in it can trap, so LLVM proves it has no observable effect and deletes two hundred million iterations outright. Cranelift does not. `fib_wrapping.ls`, which has no such algebraic collapse (`objdump`: 21 real instructions), is the trustworthy number: **-23.3%**, in the same range as #102's other kernels -- `overflow-cost.md`/`check-cost.md`'s "an observable trap is not reassociable" finding, seen this time from removing the trap rather than adding one |
| [#104](https://github.com/alpibrusl/lex-sys/pull/104) | [LLVM backend, `region`/`alloc_slice`](llvm-backend.md) | The bigger of the two gaps #102 named: one `malloc` in, one `free` out, a bump pointer in two `ptr`-typed `alloca` cells rather than an SSA value, matching `lex-sys-codegen`'s own arena exactly (same 64 KiB `ARENA_CHUNK`). Building `sieve`/`scan` against it found two smaller gaps sitting in front, not named until tried: `byte_of` and `Expr::Not`. Every program behind only those four gaps now builds on `--backend llvm`. Measured: `sieve`/`scan` are **29%-79% faster** than Cranelift -- the first memory-bound, bounds-checked kernels this document has compared. **Checked with `objdump`, not assumed**: the wrapping halves of both pairs actually vectorise (409 SIMD instructions in `scan_wrapping.ls`, 22 in `sieve_wrapping.ls`) where their checked twins -- otherwise identical source, diffed to confirm it -- compile to zero, confirming `check-cost.md`'s "an observable trap is not reassociable" on a bounds-checked memory scan, not only arithmetic, on a real backend for the first time |
| [#105](https://github.com/alpibrusl/lex-sys/pull/105) | [LLVM backend, heap boxing](llvm-backend.md) | `box_slice`/`contents`/`unbox_slice`, sharing `alloc_slice`'s own checked-multiply sizing and reaching for `malloc` instead of `bump`. Found a real bug along the way: `Box`'s own leaf count was never special-cased the way `lex-sys-codegen`'s `abi::leaves_into` already does, so `Box[[T]]` silently scalarised wrong. Building `reduce_checked.ls` against this found a second, unrelated gap underneath: a function could not return more than one leaf, closed in general (`struct_ty`/`pack_struct`, an `insertvalue`/`extractvalue` aggregate, the same shape `checked_arith`'s own `{i64, i1}` already is) rather than scoped to two leaves. Every program behind only these gaps now builds on `--backend llvm`. Measured: `reduce_checked.ls` -- `docs/gpu.md`'s own kernel -- is **61% faster** than Cranelift, and `objdump` confirms the vectorisation split a fourth time (`reduce_wrapping.ls`: 54 SIMD instructions, `reduce_checked.ls`: zero) |
| [#106](https://github.com/alpibrusl/lex-sys/pull/106) | [LLVM backend, `getchar`/`s[a..b]`](llvm-backend.md) | `getchar`, the mirror of `putchar`. Trying it against `revcomp.ls` found a second gap in front: `s[a..b]` (`Expr::Subslice`), the same two bounds checks `element_address` already makes, over a range. `tests/accept/stdin_roundtrip.ls` -- the repository's own first `//~ STDIN` fixture -- closes the loop, piped and diffed byte-for-byte against Cranelift. **`revcomp.ls` itself still refuses, and its boundary moved a second time in the same session**: past both closed gaps it reaches `std.buffer`'s `Buffer.clear`, which needs `Place::Field`/`Place::Deref` -- named since §5's fourth slice, materially bigger than either gap closed here, and not closed. `docs/llvm-backend.md` §7.10 connects it to `revcomp.ls` explicitly for the first time |
| [#108](https://github.com/alpibrusl/lex-sys/pull/108) | [LLVM backend, field/deref access](llvm-backend.md) | The gap #106 found and did not close: on contact, nine IR nodes rather than two -- the named writes (`Place::Deref`, `Place::Field`), plus a read-side cluster nothing had built (`Expr::Deref`, `Expr::FieldRef`, `Expr::FieldAddr`, their tuple-shaped counterparts), plus `Expr::Tuple`/`Expr::TupleField`, never built either. All nine share two new helpers, `field_offset`/`tuple_field_offset`, computing a field's byte offset once rather than five times over. Building `revcomp.ls` against this found a third, unplanned gap underneath: `write_bytes`/`write_err` (`fwrite` through `stdout`/`stderr`), needed once `std.buffer`'s own field writes stopped being the reason it refused. **`revcomp.ls` itself now builds, runs, and matches the Benchmarks Game's own published output** on `--backend llvm` -- the boundary that moved twice in #106 is finally past. Measured: **42%-46% faster** than Cranelift; `objdump` finds 23 SIMD instructions, real but modest, attributed to the program's `getchar`-based byte-at-a-time read side rather than this slice's own work. `docs/llvm-backend.md` §7.12 leaves three gaps: bare `alloc`, `arg_count`, `Type::Float` -- plus matching through a reference, named for the first time as its own distinct, still-unbuilt item rather than bundled with the field/deref access this slice closed |
| [#109](https://github.com/alpibrusl/lex-sys/pull/109) | [LLVM backend, `arg_count`/`arg`](llvm-backend.md) | `argc`/`argv` stashed once into module-local storage by `@main`, mirroring `lex-sys-codegen`'s own `emit_c_main`; `arg`'s bounds check the same `icmp uge` shape every other indexing operation here already uses, then `argv[n]`'s length computed with libc's own `strlen`. **Only one of the gap's two named targets builds past it**: `fannkuch.ls` does, checked against a real argument and not only the no-argument fallback; `binarytrees.ls` reaches bare `Expr::Boxed` instead, a different and still-open gap this slice does not touch -- the same "a gap can block more than one program and close only some of them" shape #106 found with `revcomp.ls`. Measured: `fannkuch.ls` is **22%-28% faster** than Cranelift; `objdump` finds 16 SIMD instructions, a smaller number than `scan`/`sieve`'s, consistent with `fannkuch`'s array rotation not being the loop shape those vectorise on. `docs/llvm-backend.md` §7.14 leaves two gaps with a `benches/` program behind them (bare `alloc`/`Boxed`/`Unboxed`, now including `binarytrees.ls`; `Type::Float`) plus matching through a reference, still with none |
| [#110](https://github.com/alpibrusl/lex-sys/pull/110) | [LLVM backend, single-value `alloc`/`box`/`unbox`](llvm-backend.md) | Closes the gap #109 found underneath `binarytrees.ls`: bare `Expr::Alloc`/`Expr::Boxed`/`Expr::Unboxed`, single-value allocation, arena or heap. Each one a smaller version of a primitive this backend had already built for a slice's many elements -- `alloc_slice`'s own `bump`, `boxed_slice`'s own `malloc`-and-null-check, both minus their fill loop -- sharing a new `value_bytes` helper, the whole-value counterpart of `stride_of`'s per-element version. **`binarytrees.ls` builds too now, taking both of #109's named targets with it rather than only one** -- checked with no argument and with a real one, matching Cranelift exactly both times. The crate's and the CLI's "outside this backend" refusal tests moved from `arena_roundtrip.ls` (now inside the backend) to `floating_point.ls`, the next real boundary. `docs/llvm-backend.md` §7.16 leaves one gap with a `benches/` program behind it: `Type::Float`, now the only gap left blocking more than one (`spectral.ls` and `fasta.ls` both) -- plus matching through a reference, still with none |
| [#111](https://github.com/alpibrusl/lex-sys/pull/111) | [LLVM backend, `Type::Float`](llvm-backend.md) | The last gap with a `benches/` program behind it. The straightforward reading -- add an `F64` arm -- was wrong: this backend's `LValue` carries no type tag the way Cranelift's intrinsically-typed `Value` does, so `binop`/`Expr::Neg` had no way to tell a `float` operand from an `int` one before evaluating it. `scalar_kind`, a new structural helper, answers that without evaluating anything, walking the same shape the type checker already agreed on. Once it existed: unchecked arithmetic, ordered `fcmp` comparisons (`!=` the one unordered exception), `float_of`/`truncate` (three checks ahead of `fptosi`, which is poison rather than trapping on the inputs that matter -- no LLVM counterpart to Cranelift's trapping `fcvt_to_sint`), `bits_of`'s NaN canonicalisation, `sqrt`, and both halves of a previously-unhandled `Expr::Neg`, found unrelated and closed alongside. **`spectral.ls` and `fasta.ls`, both named targets, build and match Cranelift exactly** -- the second checked against the Benchmarks Game's own published output. Measured: `spectral(1500)` **45%-56% faster**; `fasta(1000000)` **10%-20% faster**; both real vectorisation, `float` arithmetic never having been checked to begin with. This slice's own diff pushed `emit.rs` past `CONTRIBUTING.md`'s 2,000-line budget -- split by concern into `body/{mod,arith,control,memory,expr}.rs`, the same shape `lex-sys-codegen`'s own `body/` already is, a pure reorganisation confirmed by rebuilding every fixture and `benches/` program before and after. The refusal tests moved a second time in one slice, from `floating_point.ls` (now inside) to `match_a_reference.ls`. `docs/llvm-backend.md` §7.18: one gap left, and it has had no `benches/` program behind it since §5 -- matching through a reference |
| [#112](https://github.com/alpibrusl/lex-sys/pull/112) | [LLVM backend, matching through a reference](llvm-backend.md) | The last gap this document tracked with any target behind it. Unlike every slice before it, close to what it looked like on the surface -- §7.11's `getelementptr`-address idiom and the fifth slice's `variant_layout`/`bind_payload` had already built everything it needed. A reference is always one pointer leaf, so the scrutinee evaluates the same way in both modes; only the tag read differs (one more `load` by reference), and `bind_payload` gained the address-only mirror of its by-value half -- `variant_layout`'s own offset scaled to bytes via `getelementptr`, stored as an address rather than a loaded value. Unlike Cranelift's `debug_assert_eq!`, this backend returns an `Err` on the "one leaf per by-reference binding" invariant, matching its own no-panic convention. **`tests/accept/match_a_reference.ls` and `examples/tree.ls` -- the richer target `docs/reading-references.md` §4 names by name, a three-field variant matched three separate ways -- both build and match Cranelift exactly.** A `&!` unique-reference write, with no fixture anywhere in the repo, was checked by hand instead. **Every gap this document names a target for is now closed** -- the refusal fixture moves a fourth time, to `tests/accept/bytes_to_c.ls`, refusing on a foreign call |
| [#113](https://github.com/alpibrusl/lex-sys/pull/113) | [LLVM backend, `listen`/`accept`](llvm-backend.md) | The first slice with no `benches/` program or `tests/accept/` target behind it -- every gap this document tracked closed with #112, so closing `Net` itself needed scoping directly, smallest sub-piece first, ahead of `connect`/`bind` rather than after them. Neither `listen` nor `accept` takes a capability -- a fd's authority was already proved once, at `bind`, a dedicated `Expr::Bind` IR node rather than a `Callee::Builtin` call -- so both are ordinary fixed-signature `libc` calls, no different in shape from `Sqrt`: narrow the `int` argument(s) to `i32`, `call`, `sext` the result back to `i64`. **`connect`/`bind` and `Ffi`/`extern fn` are still refused, confirmed empirically** -- a real `edition 2;` program's `bind(n, port)` names `Bind` specifically on `--backend llvm` while running to completion on `--backend cranelift` -- so there is no way to get a *real* bound fd out of a `--backend llvm` program yet, and `tests/accept/listen_accept_bad_fd.ls` checks both builtins against a deliberately invalid one instead: `EBADF`, matching on both backends, with no real socket or live connection needed |
| [#114](https://github.com/alpibrusl/lex-sys/pull/114) | [LLVM backend, `bind`](llvm-backend.md) | The second of `Net`'s four builtins, after #113's `listen`/`accept`, and the first that reaches a *real* fd rather than a deliberately invalid one. Folds `socket`, `setsockopt(SO_REUSEADDR)` and `bind` into one call, the same `struct sockaddr_in` `lex-sys-codegen`'s own `bind` builds by hand -- family bytes `2, 0` needing no platform branch, the same fact `#77`/`connect.md` §3 already measured. **The one genuinely new shape**: `socket`/`bind` can each fail, returning `-1` rather than trapping (only a bound mismatch traps, checked first) -- the first `--backend llvm` *expression* needing a value conditional on which of three runtime paths ran, where every earlier slice either trapped (nothing to merge) or was a `Stmt`-level `if` writing into an already-`alloca`'d `var`. Solved with a plain `alloca i64` result cell, written in each branch and loaded once at the merge label, the same "no `phi`" rule `if_stmt`/`while_stmt` already follow rather than a new one. **Checked against a real accepted connection**: a new differential test builds a listener on each backend, connects a real `TcpStream` from the test process, and checks both exit `0` -- the first Net-capable program `--backend llvm` has ever actually run. `connect` (`getaddrinfo`-based host resolution, genuinely larger) and `Ffi`/`extern fn` are still refused, so nothing built yet can read or write what it accepts |
| [#115](https://github.com/alpibrusl/lex-sys/pull/115) | [`examples/seek`, an agent-shaped tool](agent-tools.md) | Not a GNU port like `base64`/`cut`/`sort` -- no upstream this is checked against, because the question is not compatibility. `docs/agent-tools.md` asks whether the properties this repo already built for other reasons -- a legible authority report, no silent truncation (`line-reading.md`'s own finding about `cut`), no UB on adversarial input -- add up to a tool genuinely better shaped for an agent's own tool-use loop than a straight port would be. A literal-text search over named files, built on `std.bytes.find` and the same heap-growing `buffer`/`open_read`/`file_read` machinery `sort.ls`/`cut.ls` already use. `lex-sys authority` on it names exactly seven labels and `bounded: true`, pinned by `agent_tools.rs`. **Two honest limits found building it**: M2's `narrow` takes a literal, so no flag can narrow this tool's `Fs` to a directory at invocation time -- a supervisor gets a scoped copy by substituting the literal at build time, not a `--root` flag, the same shape `#114`'s own `bind` test already uses to substitute a port; and `-m` caps matches across every file rather than per file the way GNU's own does, because an agent bounding output is bounding its own context, not any one file's share of it. Checked on a file containing a NUL byte and a lone `0xFF`, found and printed byte for byte with no crash and nothing checked for UTF-8 |
| [#116](https://github.com/alpibrusl/lex-sys/pull/116) | [LLVM backend, `connect`](llvm-backend.md) | The last of `Net`'s four builtins, after #113's `listen`/`accept` and #114's `bind` -- genuinely the larger remaining piece, as #114 predicted. `checked_host` (mirroring `lex-sys-codegen`'s own function) is this backend's **first loop built for `Net`**: a dialled name copied into a 256-byte stack buffer while its prefix is checked byte by byte against the capability's bound, in the same "cursor in an `alloca i64` cell, no `phi`" shape the arena-fill loop already established -- not a new mechanism. `connect` then builds a `struct addrinfo hints`, resolves with `getaddrinfo`, patches the port the same big-endian way `bind` already does, and calls `socket`/`connect` -- three failure points funnelled into `bind`'s own one-result-cell shape. **Checked against a real accepted connection, both directions**: a differential test connects each backend's client to a plain `std::net::TcpListener`, and this slice's own session built an all-LLVM listener-and-client pair talking over real loopback -- the first time two programs this backend built have ever talked to each other. The two bound-mismatch traps (host outside the prefix, the exact-port check) are checked by signal, matching Cranelift. **`Net` is now fully built on `--backend llvm`.** `Ffi`/`extern fn` is the only gap left -- what a program needs to read or write what it accepted or connected to, not merely open the socket |
| [#117](https://github.com/alpibrusl/lex-sys/pull/117) | [LLVM backend, `Ffi`/`extern fn`, and a correction](llvm-backend.md) | `Callee::Extern` mirrors `lex-sys-codegen`'s own arm -- a capability parameter never crosses to C, everything else crosses at lex-sys's own widths (an `int` is `i64` regardless of the C function's own parameter width) -- reusing `Callee::Fn`'s own call-and-unpack shape, factored out as `emit_call` once both needed it; `scalar_kind` gained the `Callee::Extern` arm it was missing. Checked against `tests/accept/bytes_to_c.ls` **unmodified** (a `&r [byte]`-crossing `write`) and a fresh `labs(-5) == 5` on both backends. **Checking real programs against this slice found #116's own "`Ffi`/`extern fn` is the only gap left" claim false** -- `Fs` (`fs_read`/`fs_write`/`open_read`/`file_read`) had been unbuilt the whole time, unnamed because no slice had tried a real `Fs`-using program against this backend until `examples/seek/` did here. Corrected in place across `lib.rs`, `llvm-backend.md`, `docs/README.md` and `README.md` rather than left standing. The boundary fixture moves a sixth time, to `tests/accept/file_handle.ls`. A second finding: a user-declared `extern fn` naming a symbol this backend already declares for `Net` (`socket` et al.) at a different width makes `clang` correctly refuse to link -- not new, the identical exposure `#92`'s own entry above already recorded on Cranelift for `close`, left unfixed here for the same reason: none of the existing network examples needs a different socket call than this backend already declares for `Net` itself |
| [#118](https://github.com/alpibrusl/lex-sys/pull/118) | [LLVM backend, `Fs`, and a byte-comparison bug it found](llvm-backend.md) | `checked_path` is `checked_host`'s own loop (#116) plus a `..`-traversal refusal and a `/`-boundary check, reused by `fs_read`/`fs_write`/`open_read`; `file_read` and a per-thread `errno` accessor round it out. `tests/accept/file_handle.ls`, unmodified, builds and matches its own directives exactly on `--backend llvm`. **Checking real programs found two things, and both are fixed rather than documented away**: `read`/`write`'s own unconditional declares broke `bytes_to_c.ls` -- a program that **had already been working** (#117), unlike `socket`/`bind`'s already-accepted exposure -- fixed by declaring them only when a program's own `extern fn` doesn't already claim the symbol. And `compare` had hardcoded `icmp {cc} i64` regardless of operand type since this backend's *first* slice -- silently correct for `int`, silently ill-typed for `byte`/`bool` -- caught only once `examples/cut/`/`examples/seek/` exercised `std.bytes.find`'s raw byte comparison, which nothing in eighteen prior slices had. Fixed by threading `binop`'s own `lhs_kind` through to `compare`, checked directly (`byte_comparison_uses_the_right_width`). **The boundary fixture moves a seventh time**, from `bytes_to_c.ls` (inside since #117) to `tests/accept/static_data.ls`, refusing on `Expr::Static` -- `docs/compile-time-data.md`'s whole feature, found unbuilt the same way `Fs` was, by checking rather than assuming this backend's `Expr` match was exhaustive |
| [#119](https://github.com/alpibrusl/lex-sys/pull/119) | [LLVM backend, `Expr::Static`, `Expr::BitNot`, and the last gap](llvm-backend.md) | `Expr::Static` lays each `static`'s already-evaluated values out as one read-only global, packed at the same byte-or-eight-byte stride every other slice in this backend uses; a reference to it costs only the symbol name, declared once for the whole program rather than once per reader the way a string literal is. `tests/accept/static_data.ls`, unmodified, matches Cranelift byte for byte. **Checking whether that was really the last gap -- by walking every `Expr` variant against this backend's own match, rather than trusting the running "named gaps" list a fourth time -- found `Expr::BitNot` unbuilt too**, invisible until now because `tests/accept/bitwise.ls`'s only use, `~0`, is a literal the checker folds away before codegen ever runs. Fixed as `Not`'s own `xor`, at `i64`/`-1` in place of `i8`/`1`; checked against a value `putchar`'s own echo keeps unfoldable. **With both closed, `expr`'s match has no unhandled `Expr` variant, so its wildcard "not built yet" arm came out entirely** -- `rustc` itself now refuses to build this crate if a future `Expr` variant goes unmatched, a stronger guarantee than any test here gave before. The same enumeration over `Callee::Builtin` found nothing missing either. **The boundary fixture does not move an eighth time**: every file in `tests/accept/` and every program in `examples/` was built against `--backend llvm` directly in this slice's own session, and all of them succeed. The one program that still refuses, `examples/collect/collect.ls` (and `fetch/`/`report/`/`serve/` alongside it), does so for `#92`'s own already-accepted `extern fn socket` collision -- not a missing node |
| [#121](https://github.com/alpibrusl/lex-sys/pull/121) | [`std.crypto`, SHA-256, and a `static` never gated by reachability](crypto.md) | The first slice of moving `lex-os` off Rust: `crates/lex-os-audit`'s hash-chained log calls `sha2::Sha256` on every entry, so a port needs a hash function to exist first. FIPS 180-4, in masked 64-bit `int` arithmetic -- `mask32(x) = x & 0xffffffff` at the points the spec says to reduce mod 2^32, so the checked `+` underneath never traps and no `wrapping_add` is needed. The round constants are two `static` items -- the first real consumer of `Expr::Static` outside its own design doc and `tests/accept/static_data.ls`. Checked against four vectors computed with the system `sha256sum` rather than from memory, including the 56-byte input that forces the multi-block padding path this backend's earlier fixtures never reached; `tests/accept/sha256.ls` matches on both backends. **Building it against `AGENTS.md` §7's "an unused declaration costs nothing" test found a real gap `static_data.ls` could never have exposed on its own**: unlike a function, a `static` was never gated by `mono`'s reachability worklist -- every `static` a program declared was evaluated and pushed into `Program::statics` whether or not anything read it, so `std.crypto`'s tables reached the object file of any program that merely `import`ed the module. Fixed in `lex-sys-ir`: an exhaustive, no-wildcard-arm walk finds which `static` indices a reachable function actually names, closes over statics reading earlier statics (safe, since a `static` may only read one declared before it), and renumbers what survives before evaluation runs -- checked against `static_data.ls`'s own cross-static reference and a fresh case where the *middle* of three statics is dropped and the third has to renumber past it, on both backends |
| [#122](https://github.com/alpibrusl/lex-sys/pull/122) | [`c_int`, fixing a real bug in `extern fn`'s foreign return](reach.md) | Scoping `examples/vsock/` -- connecting over `AF_VSOCK`, the channel `lex-os-guest` uses to reach its host supervisor -- found a real, previously-undiscovered bug in both backends, not specific to vsock: a foreign call declared `-> int` read the C function's return as a full 64-bit register. Correct when the real C function genuinely returns 64 bits (`labs` returns `long`, why its own test never caught this), but most libc calls (`close`, `connect`, `socket`, `bind`, `listen`, `accept`) return a 32-bit C `int`, and a failing call can leave the upper 32 bits of the return register zero rather than sign-extended -- `close(999)` read back as `4294967295`, not `-1`, confirmed byte-for-byte against equivalent C. **No universal fix exists**: `extern fn`'s syntax cannot distinguish a 32-bit `int` from a genuinely 64-bit `long`/`ssize_t`, both spelled `-> int` today, so assuming either width for every declaration would fix one case and silently break the other. `c_int` names the real width explicitly, in return position only -- the call crosses at 32 bits and sign-extends, and the value is exactly `int` everywhere else in the program. Every existing socket declaration across `examples/serve/`, `fetch/`, `report/` and `collect/` is migrated; `read`/`write` stay plain `int` (`ssize_t`, genuinely 64-bit). **Their own `< 0` checks had this exact bug the whole time**, untested only because the shipped suite exercises success paths, never a real `bind`/`accept` failure. `examples/vsock/` is fixed and unblocked by the same change; `docs/net.md` §5's outbound count moves to 3 |
| [#123](https://github.com/alpibrusl/lex-sys/pull/123) | [`std.crypto`'s second slice, SHA-512, and the logical shift it needed](sha512.md) | Ed25519 (`lex-os-capsule`'s signing need) hashes with SHA-512 (RFC 8032), not SHA-256 -- `crypto.md` §7 named the dependency and deferred it. `compress512` is `compress()` again at double the width and eighty rounds; `wrapping_add` does directly what `mask32(checked +)` had to simulate for SHA-256's masked word. **A full-width word surfaced two gaps SHA-256's narrower one hid.** Every rotate SHA-512 needs is logical, and this language's `>>` is arithmetic by design (`bitwise.md` §2) -- built the logical shift out of the arithmetic one by masking off exactly the bits sign-extension might have gotten wrong, and `low_mask64(63)` needed its own literal since `(1 << 63) - 1` traps. And a 64-bit literal with the top bit set **does not parse at all**: every such constant in `sha512_h0`/`sha512_k` is spelled `(hi << 32) \| lo` instead, each half reconstructed and checked against the original value before being written down -- a mechanical transform, not a per-constant judgement call. `H0` and the round constants are independently derived (integer square/cube roots) and checked against Python's `hashlib.sha512` and the system `sha512sum` on four vectors; both backends agree, and `std_declarations_cost_nothing_unless_called` needed no change since SHA-256's reachability fix already generalized. §5 names what Ed25519 still needs beyond this -- field arithmetic `std.bignum` cannot yet do, curve point operations, a constant-time posture -- and deliberately starts none of it |

### The pattern, if there is one

Twenty-one of the first twenty-nine slices found a bug, falsified a claim
the project had already written down, or both — and three of those were
soundness bugs reachable from ordinary code. Re-counted over the
twenty-one slices since, on the same criterion and by reading the rows
above: **seventeen did**, so thirty-eight of fifty. The four that did not
are `utf8.md`, `AGENTS.md`, the `sort` port and the backend audit — the
last of which checked four claims and found all four true, which is the
outcome this paragraph should be able to record without the count
looking like a failure. That is not an accident of luck: each
slice is built by writing the thing the previous document said was
possible, and the documents keep being wrong in the same direction —
optimistic about what generality the type system already had.

The convention that follows is worth stating, because it is why the
documents are trustworthy at all: **a falsified claim is corrected in
place, in the document that made it, rather than quietly edited.**
`sharing.md` corrects `linearity-and-effects.md` §9;
`collections.md` corrects both `standard-library.md` §4 and
`mode-polymorphism.md` §1, the second of which was itself a correction;
and #36 corrects `reading-references.md` §2.0 and `sharing.md` §2.1,
where a refusal turned out to be broader than the hazard it was written
for; and #41 corrects a refusal that named `()` as a foreign result, in a
language whose grammar refuses `()` on purpose — two correct rules with a
loop between them, which only a program walking into it would find; and
#42 corrects `README.md`'s performance expectation and
`defined-behaviour.md` §2.1, which had put a number on the overflow trap
that nobody had ever measured — the one claim here that an outside reader
caught before the project did. And #55 corrects `canonical-ast.md` §8 —
the section whose whole job is to say what is *not* a contract — which had
described a struct literal's field order as a gap it does not have and
given the wrong reason for the one gap it did have. A wrong entry there is
worse than most, because it is the entry everyone else reads to find out
what they may rely on.

---

## What is next

| Next | Why it is next |
|---|---|
| ~~Does `&!` mean unique?~~ | **Answered, no** — [`aliasing.md`](aliasing.md). This row priced the decision as *"it refuses programs that compile today"*, and across all 82 programs the repository builds it refuses **one fixture**, written to document the behaviour being changed. The price is elsewhere, in two places that each settle it alone. There are **three** ways to alias a `&!`; two close syntactically and the third, `both(head(s), s)`, does not — nothing in `head`'s signature says the result borrows its argument, and a shared region is a shared *arena*, not a shared object, so carrying that fact across a call is lifetimes and a borrow checker, which the README's design commitments rule out by name. And even proved, the fact has no consumer: Cranelift has no `noalias`, only three fixed WebAssembly alias regions. The two conditions that would reopen it — an LLVM backend, and parallel lanes to make it a *correctness* argument rather than a speed one — are §6 there |
| ~~A vectoriser, or a backend with one~~ | **Built** — [`llvm-backend.md`](llvm-backend.md). [`layout.md`](layout.md) §3 and [`overflow-cost.md`](overflow-cost.md) §3.2 turn out to be the same wall wearing two hats: a trapping add is not reassociable, so the loop does not vectorise, so neither contiguity nor reassociation can be spent. [`bulk-io.md`](bulk-io.md) §4 puts a third hat on it — a byte-at-a-time slice copy costs 6 ms per 8 MB here and nothing measurable in C, which is now the largest single thing between `base64/` and coreutils. It is the one thing that would move several of these numbers at once. And [`check-cost.md`](check-cost.md) says where the traps are in the way: **six of eight** loop-body checks make their operation non-reassociable, so a backend with a vectoriser would collect the gain only outside those, and the list of places is §2's table there. And [`backend-limits.md`](backend-limits.md) checks the case against the backend rather than from memory: there is no aliasing fact to emit, no purity fact to emit, and no pass that makes SIMD out of scalar code — each a property of Cranelift's design rather than a version it has not reached. **The three arguments for LLVM are one argument**: this language computes facts its backend has no vocabulary for. **How, not whether, is now settled, and its first five slices are built — `hello.ls` itself now builds through it.** [`llvm-backend.md`](llvm-backend.md): textual IR shelled out to `clang`, no new build dependency, and a spike that found the obvious way to emit the overflow trap raises the wrong signal on one target (confirmed on both this project's targets by this backend's own tests). §5's doorway, checked arithmetic, control flow (needing **no `phi`**, because every local here is already memory), slices/strings, and structs/enums (`match` reusing the same memory-backed reasoning a third time) are all `lex-sys-codegen-llvm` behind `--backend llvm`, checked against the Cranelift path's own output — `examples/hello.ls`, `ci.yml`'s own smoke test, was the fourth slice's own target, byte for byte the same through both backends. **Whether `clang -O2` actually vectorises a loop-heavy checked-arithmetic kernel through this backend is now measured, and the answer is no** (#102, `llvm-backend.md` §7): `objdump` on the `-O2` object for both `sum_checked.ls` and `mandelbrot.ls` finds zero `%xmm`/`%ymm`/`%zmm` instructions — this document's and `check-cost.md`'s own "a trap is not reassociable" finding holds on a real backend, not only Cranelift's absence of one. What `-O2` bought instead was scalar: `run_clang` had never passed `clang` an optimisation level at all (a real bug, fixed), so `mem2reg` never ran and every leaf stayed a real stack slot — once fixed, register allocation and instruction selection alone are enough to put lex-sys/llvm within noise of C on `mandelbrot.ls` (0.96×–1.00×, against Cranelift's 1.6×–1.8×) and 8%–54% ahead of Cranelift on the three kernels that build on both backends today. #103 closed `wrapping_add`/`sub`/`mul` and found that *removing* an observable trap lets LLVM delete a whole no-op loop outright, the same fact from the other side. **#104 is where the vectorised half of this row's own claim is finally measured, not just theorised**: `region`/`alloc_slice` closed, `sieve`/`scan` build on both backends, and their wrapping halves actually vectorise (409 SIMD instructions in `scan_wrapping.ls`, 22 in `sieve_wrapping.ls`) where their checked twins compile to zero, `objdump`-confirmed. So the claim holds in both directions now, not only the negative one: a checked loop does not vectorise, and the *same* loop with the trap removed does. #105 closed heap boxing and, underneath it, multi-leaf function returns (`reduce_checked.ls` — `docs/gpu.md`'s own kernel — confirms the same vectorisation split a fourth time). #106 closed `getchar`/`s[a..b]` and found `revcomp.ls`'s own boundary is `Place::Field`/`Place::Deref`, not `getchar` as assumed. **#107 through #119 closed everything that was left**: field/deref access and `revcomp.ls` itself (#108); `arg_count`/`arg` and `fannkuch.ls` (#109); single-value `alloc`/`box`/`unbox` and `binarytrees.ls` (#110); `Type::Float`, the last gap with a `benches/` target (#111); matching through a reference, closing every gap this document had a target for (#112); then `Net` itself, scoped directly rather than by a target — `listen`/`accept` (#113), `bind` (#114), `connect` (#116), **fully built on both backends**, checked against a real accepted connection and an all-LLVM listener-and-client pair talking over real loopback; `Ffi`/`extern fn` (#117), which found the "only gap left" claim false three PRs running before it — `Fs` had been unbuilt and unnamed the whole time; `Fs` itself (#118), closed alongside a byte-comparison bug hardcoded since this backend's first slice; and `Expr::Static`/`Expr::BitNot` (#119), the last two `Expr` variants, closed alongside the discovery that the backend's own `expr` match was not actually exhaustive despite four separate "only gap left" claims saying so. **With #119, `body/expr.rs`'s match has no unhandled `Expr` variant and no wildcard arm** — `rustc` itself now refuses to build the crate if a future variant goes unmatched, and every fixture in `tests/accept/` and `examples/` builds on `--backend llvm`. The one thing still refused, a user's own `extern fn socket` colliding with this backend's internal declaration, is `#92`'s already-accepted exposure repeating itself, not a missing feature |
| ~~`exp`, `log`, `pow`~~ | **Built** — [`float-math.md`](float-math.md) §7. Three programs asked by name (`examples/growth.ls`, `decay.ls`, `entropy.ls`), clearing the two-asker bar this row itself said was unmet. Library code in `std.math`, checked against Rust's own `f64::exp`/`f64::ln`/`f64::powf` within 1e-9 relative error — not correctly rounded, the same honest limit `sqrt` itself could not clear as a hand-rolled function. `sin` stays open: nothing has asked, and it needs its own range reduction |
| ~~File handles~~ | **Built** — [`file-handles.md`](file-handles.md). `open_read`, `file_read`, `file_close`, a `res` `File`, and a three-constructor `Read` so the end of a file and a failure stop being two meanings of `-1`. Building it settled the three things §6 left open and found one the design had not seen: `Result[File, int]` cannot be a builtin's signature, because `Result` is `std` and `std` is opt-in rather than a prelude. `Failed(int)` now carries the real `errno`, which is the one argument for handles that survived §1's other two evaporating. The price nothing had counted: three prelude names cost 31 fixtures and the `extern fn read` in `examples/serve/`, so the two verbs are `file_read` and `file_close` rather than `read` and `close` |
| ~~fasta and reverse-complement~~ | **Built** — [`benchmarks-game.md`](benchmarks-game.md) §7. The Game's spec was reachable this session, and the measurement corrects the prediction rather than confirming it: `fasta` is not nearer `base64`'s 1.59×, it is **faster than C**, at 0.54× |
| lex-sys code in `lex-vcs` | lex-lang's `crates/lex-vcs` is **81% language-agnostic already** — the op DAG, merge, attestations, intents and signing all key on `String` ids and `BTreeSet<String>` effects, and only ~1,500 lines (`compute_diff`, `diff_to_ops`, `body_merge`) walk a Lex `CExpr`. The gate is two lines from being a trait. What is not ready is this side: §8 above. #55 started measuring how often these hashes move; the design doc is worth writing when that number exists and §8 is nearly empty, not before. One thing to think about first — `body_merge` auto-merges disjoint subtrees, and in a linear language two edits that each typecheck can together move a value twice, so it will conflict far more often here than it does there. **The number it was gated on now exists** — [`hash-stability.md`](hash-stability.md). It is two numbers and they disagree: the encoder has moved 20 times and the golden fixtures observed **none** of them, while the *language* has moved enough that **71%** of this repository's own `.ls` history no longer type-checks, 42% of it from one label rename. An op DAG on these hashes would mostly record the language changing underneath programs nobody touched. So the instinct to wait was right, and what to wait for is not a number but a **plateau** in the effect vocabulary and the builtin surface — which the last four slices did not provide. **That plateau has since happened, measured rather than assumed.** `crates/lex-sys-ir/src/builtin.rs` (every builtin) and `crates/lex-sys-ir/src/defs.rs` (every effect label, including the prelude types' own rows) last changed at #92, the third `Net` slice; `git diff 52daddb..2999a7d -- crates/lex-sys-ir/ crates/lex-sys-types/ crates/lex-sys-syntax/` is **empty** — **27 commits (#93 through #119), zero changes to any file that declares a builtin, an effect label, or a prelude type**, while an entire second backend (`llvm-backend.md`) was designed and built behind it start to finish. `hash-stability.md` §3's own "this repository has not had one yet" is now the claim this row corrects in place: it was true when written and is not any more. Whether 27 commits is *enough* of a plateau — long enough that a `lex-vcs` design doc would not immediately need revising — is the open question this row now asks, not whether one has happened at all |
| ~~A line reader~~ | **Answered, no** — [`line-reading.md`](line-reading.md). This row said `examples/cut/` and `examples/tally.ls` *"both read `getchar` into a fixed buffer"*, and **`tally.ls` has no buffer**: it streams. Five programs call `getchar`, one of them reads a line at a time, and the bar here is two. What the row was right about is that something was wrong: `cut` was **silently** printing the wrong field on a long line — a truncated line loses its delimiters, so it switched to the no-delimiter rule and answered 60 KB of `a` with exit 0. Fixed by growing on the heap, at 1.18×, and the one function that *was* earned is `buffer.clear`, at 13.0× against the alternative |
| ~~Effect polymorphism~~ | **Answered, no** — [`effect-polymorphism.md`](effect-polymorphism.md). This row and `mode-polymorphism.md` §8 were the entire design, and counting by reading says the feature has nothing to quantify over: **537 functions here write a row and not one could be polymorphic in it**. A row is fixed at the declaration (`Signature.effects`, read straight off it by every caller), and the two things a function is generic over — types and regions — cannot select a callee. The nearest asker, `io.write_all` against `io.error_all`, differs by the *builtin* as well as the row, so collapsing it needs a function value: row polymorphism is the shadow of higher-order code, and `Rule::NoFunctionValues` refuses that with a fixture |

An LLVM backend is **not** next, and the case for it is now argued from
two directions at once — which is worth keeping written down, because
the two arrive at opposite conclusions about its *priority* and agree
about its *necessity*.

`purity.md` §4.2 shrank it: the effect row's advantage needs a
compilation boundary, lex-sys compiles whole programs, and LLVM infers
the same fact itself when it can see every body. So the row buys less
than it looked like it would.

[`gpu.md`](gpu.md) §2.3 grew it, from somewhere else entirely. Asked
what a GPU would cost the design, the measurement found that **deleting
the overflow trap does not reach C**: lex-sys without traps is still
scalar, 2.27× off vectorised clang and 1.55× off *scalar* clang, so the
trap is 1.83× of its own cost and everything remaining is Cranelift.
Removing the trap is necessary and not sufficient. That makes LLVM the
prerequisite for two things rather than one — the CPU gap, **and any
GPU future at all**, whichever language the kernels would be written in.

It remains the answer to the 1.17–2.58×, and that is a performance
problem in a language that is not yet usable. But it is no longer only
a performance problem: it is the gate in front of the one direction
this project has been asked about and cannot currently go.

`jo` instead of `seto`/`test`/`jne` (`overflow-cost.md` §3.4) was listed
here as ordinary work **blocked by nothing**, and
[`emitted-checks.md`](emitted-checks.md) §2 says otherwise: that sequence
is Cranelift's x64 lowering of `trapnz`, so it is an upstream change
rather than one this repository can make. It stays worth doing and is
not ours to do.

Flag parsing has moved off this list and into
[`flags.md`](flags.md) — and it was not the small thing this line called
it.

**Standard error is done** — [`standard-error.md`](standard-error.md).
This list called it *"a gap `reach.md` §6 upgrades from an omission to a
named gap"*, and `reach.md` §6 never mentioned it; the citation was
copied from `file-handles.md`, which had invented it. Closed as a third
label on `Io`, after measuring that every failing program here produced
zero bytes on every stream and that the workaround both corrupts a
pipeline and loses the message on a trap.

Environment variables moved off that list and onto `reach.md`'s: `getenv`
returns a `char *`, so they are not reachable through FFI at all and want
a capability with builtins, the way `Args` has.

---

## Deliberately excluded

Borrow checker, traits, `comptime`, an own optimiser, incremental
compilation, LSP, async.

Each is "yes, later" rather than "no". Saying yes early is what turns
three months into three years, and the
[non-goals](../README.md#explicit-non-goals) say which of them are "no"
outright.

---

## Beyond

The first real target is **`lex-os`** — production systems work, no
rewrite risk.

Self-hosting the lex-lang toolchain stays a *spike before a plan*: port
`lex-ast`/`lex-vcs` canonical forms and verify byte-identical
`OpId`/`SigId`/`StageId` over the existing ~136k-op corpus, then decide.
